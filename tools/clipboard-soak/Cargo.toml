[package]
name = "clipboard-soak"
version = "0.1.0"
edition = "2021"
publish = false
description = "clipboard-soak — the long-run clipboard evidence rhydra tranche 5's AC8 asks for"

[[bin]]
name = "clipboard-soak"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[workspace]
