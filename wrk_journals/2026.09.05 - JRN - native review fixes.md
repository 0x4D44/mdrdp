# Native review fixes — 2026-09-05

Arthur authorized fixing and integrating the three defects from the native transport review: MDR-BUG-FLU-00126 (ACK failure notification), 00127 (control response bounds), and 00128 (partial auxiliary startup cleanup). The older scripted-duration bug 00125 is outside this request.

Each fix uses its own claimed worktree and integration. No dependencies or wire-format changes are planned. The fixing session leaves records Fixed for independent verification, not Closed.

## MDR-BUG-FLU-00126 — ACK failure notification

The source diagnosis is an ordering error: ACK write failure sets stop before any worker sends a terminal window notification. Sibling workers consequently treat the failure as an intentional shutdown. The fix must preserve both the close notification and the failure reason, while keeping intentional shutdown quiet.

Implemented one atomic terminal claim shared by ACK/video/input workers. The ACK thread now returns its failure through its join handle; native shutdown retains that reason as `TransportFailed`. Socket shutdown and the window notification occur only for the winning terminal failure.

Regression evidence: the worker restored the old ACK stop-without-notification behavior and observed the ACK result assertion fail (`WindowClosed` instead of `TransportFailed`). The lead additionally replaced the atomic claim with an unconditional successful claim: intentional ACK shutdown failed its `WindowClosed` assertion and the competing-failure test observed three notifications instead of one. Both mutations were removed. Every new regression was observed failing on an assertion.

The lead reran `deltic timeout 180 cargo test --lib native::session </dev/null`: 60 passed. Windows type-check passed for both mdrdp and Rhydra host; it retains unrelated warnings about the missing cross-build icon tool and unused dimensions in `~/language/mdrdp/src/present.rs:95`. Formatting and diff whitespace checks passed. The worker also ran warning-denying library/test clippy and repeated the contention test 20 times successfully. This is focused failure-path validation, not a live GUI disconnect or a full test-suite run.

CLI smoke: `deltic timeout 180 cargo run --quiet --bin mdrdp -- --help </dev/null` exited successfully and printed the expected options.
