# UI handoff → egui build (loop-build)

Worktree: `~/worktrees/mdrdp/20260816-TSK-HUM-ui-handoff-egui-shell-launcher-wizard-se`
Branch: `task/20260816-TSK-HUM-ui-handoff-egui-shell-launcher-wizard-se`

**Spec of record:** `wrk_docs/2026.08.16 - HANDOFF - launcher and diagnostics UI/README.md`
(high fidelity — colours/sizes/copy are final; read it, not the dc.html files, for values).

## Decisions taken
- **Toolkit: egui** (Arthur, 2026-08-16, via session question; handoff allowed iced or egui).
- **Menus: muda** (handoff, settled).
- Old `src/ui/{form,list}.rs` are superseded once the new shell renders; `font.rs` stays
  while `window.rs::draw_overlay` still uses it. `FormError` wording must be reused.
- Hosting architecture (eframe vs manual egui-winit+egui_glow shared host): **open — settle
  in the HLD unit (U4)** with a spike compile. Constraint: session process already owns the
  one winit EventLoop (`window.rs`, `run_app_on_demand`); eframe cannot coexist there, so
  diagnostics windows need a manual egui integration regardless. Lean: one manual host
  module used by both processes.

## Unit plan (each = one committed-green unit; ✅ when done)
1. ✅ Land icon assets + handoff docs (`a67e570`).
2. ✅ This journal.
3. Icon wiring: build `assets/icon/macos/mdrdp.icns` (iconutil) + `assets/icon/windows/mdrdp.ico`;
   `build.rs` + `winresource` embedding gated on windows target (must not break the
   mandated `cargo check --target x86_64-pc-windows-msvc`); runtime window icon on
   Windows via winit; macOS dock icon at runtime (no bundle yet).
4. HLD: egui hosting architecture, dependency set (egui/egui-winit/egui_glow/glutin or
   eframe, muda), font embedding plan (IBM Plex Sans + Mono, OFL — fetch ttfs, vendor
   under `assets/fonts/` with licence file). Lead-authored doc + spike compile.
5. Theme module: design tokens (colour table, heat-ramp anchors, type roles, spacing,
   radii) → `src/ui/theme.rs` consts + egui Style; embed fonts.
6. Egui host scaffolding per HLD; empty 900×700 launcher window renders with menu bar.
7. Launcher connections list + status bar; favourites schema: last-used timestamp,
   edit/remove ops (`favourites.rs`).
8. Wizard (3 steps, validation from `FormError` wording, Tab/Enter/Esc behaviour).
9. `settings.toml` store (schema per README §Settings store; atomic replace like
   `favourites.rs`; config-dir resolution from `trust.rs`; migrate `[defaults] username`).
10. Settings modal, six panes.
11. Connect-as-modal + failure dialogs (Connecting, Could not reach, Sign-in rejected,
    Port refused) wired to `connect.rs`/`stagelog.rs`.
12. Certificate dialogs + `trust.rs` UI hook (accept-once / pin / reject; CHANGED has no
    accept path).
13. Credential dialogs (No saved password, Password prompt) + Edit/Remove connection
    dialogs.
14. Session end / Session lost / Quit-with-sessions / About dialogs.
15. Per-slot cache instrumentation: `SlotStat` in `stats.rs`, updates in `gfx.rs`
    (`surface_to_cache`/`cache_to_surface`/`on_evict_cache_entry`), carried into
    `metrics.rs` redacted report.
16. Bitmap cache diagnostics window (grid, 5 metrics, ramp, sidebar, 1 s poll).
17. Latency & drift window (shared full-scale for bars and reference lines!).
18. Channels & codecs window (stage names exactly as `stagelog.rs` records them).
19. Toasts + one `warn` overlay line in `draw_overlay`.
20. Delete superseded `src/ui/{form,list}.rs` + `mod.rs` doc, cleanup, fmt, clippy,
    full test suite, windows cross-check.
21. Fidelity sweep vs README + live smoke against a real server (budget connects;
    graceful shutdown always).

## Progress (commits on this branch)
- ✅ U1 assets+handoff (a67e570) · U2 journal (242dc1d) · U3 icons+build.rs (a6295aa)
- ✅ U4 HLD (c868845) — eframe launcher; manual egui-winit+egui_glow session host; muda;
  connect-modal drives off child `--stage-json` stdout (one logon).
- ✅ U5 deps+fonts+theme (cc8878e) — egui 0.36 API notes: App::ui(&mut Ui,..), egui::Panel
  ::top/bottom + exact_size, CentralPanel::show(ui,..), style per Theme::Dark.
- ✅ U6+U7 launcher shell + connections list (ff42696) — src/shell/{mod,widgets}.rs;
  main.rs no-args branch runs shell::run; children tracked in running: Vec<SessionHandle>.
- ✅ U15 per-slot instrumentation (bd9e0c6, Opus worker) — SlotStats/SlotStatsHandle;
  GfxHandler::slot_stats(); metrics with_slots (NOT yet populated in main.rs).
- ✅ U9 settings store (4ba14c2) — src/settings.rs + main.rs migration wiring.
- ✅ egui host + session menu (3d2a63e) — src/ui/egui_host.rs AuxWindow; window.rs
  DiagKind/DiagnosticsUis/muda menu/1 Hz WaitUntil; MDRDP_OPEN_DIAG env for scripted
  opens; live-verified on quench (ano).

## In flight
- Opus worker: src/shell/wizard.rs (U8) — integrate into shell/mod.rs when it reports
  (front page when favourites empty; N/New connection; keychain save via creds).

## Next unit
U16/17/18 diagnostics window content (worker-per-window, one at a time to avoid
src/diag/mod.rs collisions), U11 connect modal + --stage-json (lead), U10 settings
modal, U12-14 dialogs. Deviations noted: last-used captions are relative ("2h ago")
not mock dates — needs Arthur's ack; launcher View menu items (sort, running-only)
omitted until implemented; Import favourites omitted.

## Gotchas for re-entry
- `cargo check --target x86_64-pc-windows-msvc --all-targets` must stay green (repo rule).
- One winit EventLoop per process; reuse via `run_app_on_demand` (lessons_learnt).
- Never `{:?}` an ironrdp `Config`/`Credentials`.
- Live-connect budget: prefer quench (`ano` account, memory note) over temper; always
  graceful shutdown.
- Run test suites with stdin closed (`</dev/null`).
