# 2026.08.18 — rhydra tranche 1: session agent + rename (2L flow)

Arthur's go: "start the build out", formal design via the 2L (light full-flow)
process. Scope chosen: ownership-HLD sequencing step 1 (session agent + logon-task
bring-up) plus the rhydra rename, which was agreed to ride the first productization
commit.

- Worktree: `task/20260818-TSK-HUM-rhydra-productization-tranche-1`.
- Intent line: make a host's capture stack come up by itself and stay up (agent owns
  creator → device → display mode → server), under the rhydra name.
- Design: `wrk_docs/2026.08.18 - HLD - rhydra tranche 1 - session agent and rename.md`.
- Deliberately disjoint from the un-integrated Increment 4 branch (viewer internals
  only) and from tonight's measurement rig on quench (agent validated then stopped;
  rig tasks untouched until the queued A/Bs run).

Log continues as the flow proceeds: adversarial review → external (Codex) review →
checkpoint → implement → code review → validate.
