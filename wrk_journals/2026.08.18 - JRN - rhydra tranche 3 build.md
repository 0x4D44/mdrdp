# JRN — rhydra tranche 3: `mdrdp <host> --native` MVP

## 2026-08-18 (night): kickoff under the build-out loop

First tranche picked up by the standing loop (`wrk_docs/2026.08.18 - PROMPT -
rhydra build-out loop.md`). Scope from the parent HLD §9 step 3 plus the settled
connect-path answers:

- ssh spawn + port forwards (system `ssh`, no new dependency — parent HLD §4).
- The spike viewer's presenter (persistent canvas, damage-aware, Increment 4
  phase 1) embedded in mdrdp's window shell.
- The auto-detect connect path: probe for rhydra, prefer it when found, fall
  back to RDP. Per-favourite `native = auto|always|never`; `--native`/`--rdp`
  overrides. Transport always visible (title/HUD/stage-json); no silent
  mid-session fallback.

Worktree: `task/20260818-TSK-HUM-rhydra-tranche-3-mdrdp-native-mvp`.

## Understanding pass (two parallel code maps, findings that shaped the design)

- mdrdp's window shell is already transport-agnostic (`window.rs` imports no
  ironrdp); the seam is a four-handle contract: `SurfaceStore` + `Waker` +
  `WakingSender<InputEvent>` + `SessionCommand` + `StatsHandle`. Branch point
  for `--native` is the `establish` call at `src/main.rs:747`.
- `spike-viewer` depends on mdrdp by path, so lib reuse is a cycle: the
  viewer's net/sink logic ports INTO `src/native/`; mdrdp takes portable
  `rhydra` as a path dependency (framing/rects/control/input_proto; its
  windows deps are cfg-gated — verified in its Cargo.toml).
- Real scope discovery: the spike input channel is keyboard-VK-only. An MVP
  session needs mouse + full scancode map → input protocol v2 + server
  SendInput work + wire_version 3.
- SSH auth: deploy's `Ssh` uses one-shot `ssh` with no BatchMode; a GUI
  session needs key auth (BatchMode) — quench needs a key installed as host
  config (administrators_authorized_keys).

## HLD authored

`wrk_docs/2026.08.18 - HLD - rhydra tranche 3 - mdrdp native MVP.md` — one
ssh child serving probe+tunnel, transport resolution as a pure function,
version gate on the server header, sink port writing straight into
`SurfaceStore` (no separate canvas; Increment 4's damage convert deliberately
not ported — measured ~1.2 ms at parity), input protocol v2, config surface,
8 acceptance criteria.

Two adversarial reviewers launched (systems/protocol lens; product/ops/scope
lens — Opus seats). External Codex review attempted again: still
quota-exhausted until 2026-08-20 04:32 (verified live tonight); carried as
the same residual as tranches 1–2, retro pass queued.

## Review round (2026-08-19, small hours): the strongest yield of the flow so far

Both Opus seats returned (no 529s this time). Six criticals between them,
every one construction-verified against the code, all folded:

- Probe `green` is optimistic by the agent's own design — the transport
  decision now commits only on the session sockets themselves (control is a
  pre-filter; video connect + header gate + input connect, all inside the
  Auto fallback window, one 8 s deadline).
- The forward-readiness race (ssh binds -L only after auth): ECONNREFUSED
  means retry, with try_wait short-circuit.
- The probe's version check was the wrong version domain (agent crate vs
  server wire); deleted — the header's wire_version is the single gate.
- Auto probed hosts that never opted in (Security-log noise, domain lockout
  risk): deploy now writes a local host record and Auto probes only
  recorded hosts.
- Auto's RDP fallback could console-kick a live native session on the same
  host: presence gains a transport field and the fallback is guarded.
- The SSH-key story pointed at a checklist that doesn't exist: deploy's
  verify gains a BatchMode probe with an actionable warning; key install
  stays manual, documented.

Majors of note: the "store is the canvas" plan silently reintroduced the
8 MB-per-AU copy the spike had explicitly removed (fix: `adopt_pixels` /
`pixels_mut` zero-copy surface API); the input thread's bare `recv()` would
deadlock the join (window holds the sender); DPI-unaware server metrics +
a discarded display origin would skew every click; rhydra's `win` module
would link a keystroke injector into the Windows client binary (fix: `host`
feature). Full record in the HLD §11. Marked SELF-APPROVED; implementing.

## Implementation progress (2026-08-19, small hours)

Committed green units, in order (each new test proven red once by saved-copy
mutation):

1. `183c6f3` surface: `adopt_pixels` (zero-copy AU swap) + `blit_bgra_strict`
   (in-place swizzle, strict bounds) + store wrappers.
2. `4a46050` `src/native/ssh.rs`: tunnel spawn/kill, exact arg vector,
   stderr classification, readiness polling, `ProbeFailure` taxonomy with
   qualifiers + remedies.
3. `8222024` config surface: `NativeMode`, `Favourite.native`/`ssh_user`,
   `defaults.native`, `--native`/`--rdp`/`--ssh-user`, precedence
   (`native::resolve_mode` + reconcile), edit-dialog carry-over. `Always`
   fails loudly until the connect arm lands (temporary guard in run()).
4. `4e556fb` rhydra wire v3 (worker seat, verified by my own runs): input
   protocol v2 (scan/mouse/wheel kinds), `host` feature
   (default-features=false client), `query_status` timeout, 0.3.0.
5. `9129ff5` `src/native/probe.rs`: the §4.3 four-step probe on the session
   sockets; mdrdp takes rhydra (portable half). Header read returns the
   primed `Reassembler`.

In flight: worker seat on rhydra's win-side injection (v2 reader, SendInput
scan/mouse/wheel, PMv2 DPI, DesktopCoordinates plumbing, move-stats
sampling).

6. `c43ebd7` `src/native/session.rs`: `NativeSink` (exactness onto the
   store), the net + input threads, `NativeHandle`, `SessionEnd::
   TransportFailed`. Eight tests, five mutation-proven.

## 2026-08-19: the wiring lands — the client build is code-complete

Four more green units (each new logic-bearing test proven red by saved-copy
mutation; fmt + clippy -D + full suite 615/15/5 + check-windows.sh green
after each):

8. `417cda9` support pieces: `SessionPresence.transport` (serde default
   "rdp"; `--sessions` renders `host (native)`), `presence::live_native_for`
   (the Auto fallback guard's reader), `native::deployed` (the
   native-hosts.toml record gating Auto), CONNECT_GRACE 10→15 s.
9. `fa9133b` probe stage events (`ssh-spawn`/`tunnel-up`/`probe`/
   `handshake`, fed to the same LiveStage stream) + the launcher Connecting
   dialog picks its ladder from the arrived stage names (native first stage
   → native ladder; an RDP stage after native ones — the fallback — swaps
   the pendings back).
10. `c711648` **the main.rs keystone**: transport decided after the event
    loop (Never/Auto-gated-by-record/Always-with-remedy; decoder guard;
    presence-guarded fallback); everything RDP-only (secret block, EGFX
    handler, clipboard, audio, ConnectOptions/establish) behind the RDP
    arm; `Target.user` is now `Option` — the no-account error fires at
    reconcile only for `Never`, and in the RDP connect arm otherwise
    (P-M5); `enum ActiveSession { Rdp, Native }` slot; title
    " (native)", stage-json `connected.transport`, marker "native, wire
    vN", diag/channel/epilogue native arms; presence writes transport +
    ssh identity.
11. `6c5d983` deploy: BatchMode posture check after verify (warning, never
    a failure) + writes the native-hosts record (the Auto opt-in).

Viewer lockstep confirmed: `tools/latency-spike/viewer` builds clean
against rhydra 0.3.0.

## 2026-08-19 (morning): live validation on quench — most ACs green, input blocked

SSH key installed on quench as durable host config
(`C:\ProgramData\ssh\administrators_authorized_keys`, ACL Administrators +
SYSTEM only) and a `~/.ssh/config` block giving the mdrdp ssh child the
`id_ed25519_quench` identity with no `-i` needed. `ssh -o BatchMode=yes
quench exit` green.

**Passed, with evidence:**
- **AC5 (version gate)** — against the live 0.2.0 host (wire v2),
  `mdrdp quench --native` refused at the header gate: "runs wire v2 but this
  mdrdp speaks v3: run `mdrdp deploy`". Exactly the reachable-because-no-
  version-check-in-probe path (S-M11).
- **deploy 0.3.0** — `mdrdp deploy quench --force` (the live-stack guard
  fired first, as designed) went green: agent v0.3.0 stably green
  (1920x1080@240), **BatchMode verified live**, **native-hosts record
  written live** — both new deploy features exercised end to end.
- **AC1 (native session) core** — `mdrdp quench` (Auto, recorded) opened
  natively: `connected: 1920x1080, native, wire v3`, marker "native, wire
  v3", stage-json transport native. Screenshot decoded pixel-perfect (the
  real quench desktop, live clock) — proving the whole decode/exactness
  path against a real AVC stream, not a mock.
- **AC3 (auto-detect three ways)** — quench (recorded) → native; temper
  (unrecorded) → straight RDP with **no probe line** and the audio device
  opened (RDP path), then the P-M5 no-account error from the RDP arm;
  blackhole `10.255.255.1 --native` → classified `ssh-unreachable` bounded
  at 4.1 s (ssh ConnectTimeout).
- **AC6 (teardown)** — zero `ssh -N` before and after a native session; the
  tunnel is killed on shutdown, no orphans.

**BLOCKED — AC2 (real input), and it is NOT a tranche-3 code defect:**
The client→server input path is correct end to end. The new
`native: input records written: N` line shows the client putting records on
the wire; the server's reader parses them (verified: a bogus kind byte is
rejected with "unknown record kind 99", a well-formed VK/scancode is
consumed silently). But **`SendInput` on the host returns success and
produces no visible effect on the IddCx desktop.** Proven host-side, not
client-side: injecting HELLO scancodes (kinds 3/4) and VK_LWIN (kinds 1/2)
*directly to `127.0.0.1:9501`* on quench — bypassing mdrdp entirely —
also does nothing (Notepad stays "0 characters", no Start menu).

The server runs in session 2 (the interactive console, confirmed via
`query session`) at High integrity, so the module doc's documented
"session-0 / service context" cause is ruled out. The remaining suspect is
**window-station / input-desktop attachment**: a `schtasks /sc onlogon`-
launched process can sit on a desktop that is not the console's *input*
desktop, where `SendInput` succeeds into the void — the exact "succeeds and
nothing happens" the injection code only guards for the session-0 case.
The keyboard `inject()`/`inject_scan()` code is unchanged from the
measurement rig, whose typing latency was measured against **temper**, so
it is entirely possible input injection was never exercised on quench's
headless IDD setup before now.

**Next iteration — the input diagnostic + fix (a rhydra code unit):**
1. In `rhydra`'s `win/input.rs` `serve()` (or the server startup), log the
   process window station + the input thread's desktop name
   (`GetProcessWindowStation` / `GetThreadDesktop` + `GetUserObjectInformation`,
   `UOI_NAME`). Redeploy (`--force`), read the log — this *shows* whether it
   is on `WinSta0\Default`.
2. If it is not the input desktop: on the input thread, `OpenInputDesktop`
   + `SetThreadDesktop` before the accept loop (or fix the agent's launch so
   the server inherits the console input desktop). Redeploy, re-run AC2.
3. With input live, finish AC2 (typing incl. UK `"`/`\`, mouse
   move/click/dbl/right/drag, corners, scroll — the new autoinput verbs) and
   AC4's fallback-guard live bits, then §12 record + integrate.

Everything except the input ACs is validated. The blocker is a single,
well-scoped host/agent-desktop question with a concrete diagnostic.

### Where the earlier iteration left the code pointers

**All tranche-3 code is on the branch; what remains is live validation on
quench (HLD §9, the 8 ACs) and then integrate.** Order matters:

1. **SSH key install on quench** (durable host config, authorized by the
   loop prompt): generate/choose a key, append the public key to
   `C:\ProgramData\ssh\administrators_authorized_keys` (ACL: Administrators
   + SYSTEM only), over password ssh as `ano` (password = username;
   keychain service `mdrdp`). Verify with `ssh -o BatchMode=yes ano@quench
   exit`.
2. **AC5 FIRST, before any redeploy**: the live host still runs rhydra
   0.2.0 (wire v2). `mdrdp quench --native --ssh-user ano` must refuse at
   the header gate with the "run `mdrdp deploy`" remedy. Only then
   `mdrdp deploy quench` (0.3.0) — which also writes the record AC1 needs.
3. The rest of the ACs: AC1 (Auto → native, title/stage-json/presence),
   AC2 (typing incl. UK `"`/`\`, mouse, corners, scroll — Notepad +
   Explorer via autoinput or by hand), AC3 (three-way: quench native /
   temper unrecorded → zero-probe RDP / blackholed --native → deadline;
   probe cost n≥5), AC4 (agent stopped → Always hard error, Auto fallback,
   fallback-guard refusal), AC6 (teardown: no orphan `ssh -N`, slot freed,
   fast reconnect), AC8 (per HLD §9 — check the doc).
4. GUI runs on this Mac: `caffeinate -dis` alongside; a locked Mac stalls
   GUI validation — re-queue, don't count as failure. Board etiquette: post
   a claim before quench runs; one session per account.
5. Then: HLD §12 validation record + journal close-out + `deltic
   integrate` (standing authority: integrate when green).

**Client build so far is green** (606 lib + bins, clippy -D, fmt). The whole
native client pipeline exists as a library: `native::resolve_mode` →
`native::probe::establish(host, ssh_user)` → `native::session::spawn(...)`.
It is NOT yet reachable from a real invocation — `run()` still errors on
`NativeMode::Always` (the temporary guard around `src/main.rs:519`) and never
calls the native path.

**Next unit — the main.rs wiring** (the keystone; touches the working RDP
path, so fresh context):
- Branch point is the `establish(...)` call at `src/main.rs:793`. Everything
  above it that is RDP-only (the secret block `:576-602`, the gfx/clipboard/
  audio handlers `:614-694`) must move behind the RDP arm; the native arm
  reads NO keychain secret (SSH is the boundary) — so under `Auto` the secret
  lookup must move AFTER the transport is decided (review P-M5/P-M2).
- The session slot is a concrete `Arc<Mutex<Option<session::SessionHandle>>>`
  (`src/main.rs:970`); make it `enum ActiveSession { Rdp(SessionHandle),
  Native(native::session::NativeHandle) }` (review P-M3). `established.report`
  also feeds the diag Channels window (`:923-944`), channel prints
  (`:1085-1099`), exit metrics (`:982,1058-1068`), CONNECTED_MARKER
  (`:790-796`) — each needs a native arm (show "n/a — native transport").
- Native arm: `native::resolve_mode` is already computed into
  `target.native`; call `native::probe::establish`, on success build the
  window from `conn.header.{width,height}` and `native::session::spawn` with
  `mdrdp::h264::hardware_decoder()`. Under `Auto` a `ProbeFailure` → fall back
  to RDP (fetch the secret THEN); `Always` → the `ProbeFailure::remedy()`
  error. Before an Auto fallback, presence-guard a live native session to the
  same host (review P-C2) — needs the presence transport field below.
- Title base: `mdrdp — {name} (native)` where the base is formatted
  (`src/main.rs:796`ish). stage-json `connected` gains `"transport"`.
- `CONNECT_GRACE` 10→15 s (`src/detach.rs:48`); presence gains a transport
  field (`src/presence.rs`; `src/main.rs:996-998` fills port/user).
- The launcher's Connecting modal ladder is RDP-only
  (`src/shell/dialogs.rs:101-152`); make it transport-aware or a native
  connect shows 7 forever-pending RDP stages (review P-M4).

7. `a30e4b4` rhydra host-side injection (worker seat, verified by me:
   both feature states, Windows cross-link, check-windows.sh all green):
   win input v2 reader + SendInput scan/mouse/wheel, PMv2 DPI, origin
   plumbed from dxgi describe() through FrameSource::origin(), portable
   mapping fn (5 mutation-proven tests).

**Also pending:**
- `mdrdp deploy`: BatchMode verify + write the native-hosts record that Auto
  consults (§4.2/§4.4). New unit.
- Viewer lockstep: the viewer crate declares rhydra by path; the 0.2→0.3
  bump + input_proto additions must not break its build (check
  tools/latency-spike/viewer builds).
- `./scripts/check-windows.sh` — run once the rhydra dep + win injection are
  in (confirms the client compiles for Windows with the new rhydra dep).
- quench validation: ssh key install (administrators_authorized_keys),
  deploy 0.3.0, the 8 ACs (AC5 needs the live 0.2.0 refuse BEFORE redeploy).
