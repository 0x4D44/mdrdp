# Native code review — 2026-09-04

Arthur requested one coverage-ledger-driven, multi-lens review with tracked defects and docs-lane integration, but no app, tests, exploratory harness, or fixes.

Started a dedicated worktree from fetched `origin/main` at `7ef48e989010f7df0474d3ff5d331bb5152561f6`. There was no coverage ledger. Applied the requested directory rule and selected `src/native/` from six never-reviewed areas because of its remote-data and process-lifecycle boundaries.

Independent review lenses covered correctness, security, performance, reliability, maintainability, test coverage, and devil's advocacy. Lead source checks confirmed three defects: ACK error notification ordering (MDR-BUG-FLU-00126), control response bounds (00127), and partial auxiliary startup cleanup (00128). Consolidated overlapping deadline observations into the control bug. Narrowed the cleanup finding to a failure path where the application continues without the auxiliary owner; discarded the broader claim about leaks surviving process exit.

Filed all three as Open with fixes and future regression criteria, not claimed reproductions. No existing bug states changed, no code fixes, no version bump, and no Draft requirement for speculative shared commit machinery. The daily CR report records rejected/unverified candidates and source references. Coverage marks only `src/native/`; five areas remain never-reviewed.

Validation for this docs-only unit: inspect the exact diff and ledger metadata, check whitespace and docs-only path scope. Runtime and test verification intentionally excluded. Integration uses the docs lane so the fix loop can see the new Open records on `origin/main`.
