# Clipboard files, Mac keyboard mode, and microphone

- 2026-09-25: Started a task worktree from `origin/main`. User authorized integration
  as green units complete. Scope covers ordinary RDP on macOS and Windows.
- Inventory: `arboard` 3.6.1 exposes file lists on both platforms; IronRDP CLIPRDR
  exposes file descriptors and FileContents requests. Current `src/clipboard.rs`
  deliberately disables that path. `cpal` is already a dependency, but the installed
  IronRDP 0.17.0 tree has no audio-input channel implementation. Asked Arthur whether
  a small local MS-RDPEAI channel is acceptable and whether folders are required.
- Next: review the draft design, resolve its pending decisions, then self-approve and
  implement focused green units. Do not integrate an unvalidated code unit.
- Internal adversarial review: two reviewers checked clipboard and input/audio, then
  refuted each other's top findings against code. All recorded correctness findings
  survived; the design now specifies timeout drive, file validation/correlation,
  native input separation, and IME ownership. One Claude Fable review ran and found
  cross-session staging deletion and eager-download UX risks; both were folded in.
  No extra review panel was run. Arthur's microphone/folder decisions remain pending.
