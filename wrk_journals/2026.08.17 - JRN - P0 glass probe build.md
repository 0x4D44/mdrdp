# 2026.08.17 — P0: `probe glass` build (low-latency exploration, phase 0)

Executes P0 of the signed-off HLD (`wrk_docs/2026.08.17 - HLD - low-latency server
exploration.md`) plus P1 step zero (quench RTT baseline).

## Decisions taken at build start

- **No new bootstrap machinery.** `probe::stats::summarise` already carries a
  distribution-free 95% CI on the median (order statistics). That satisfies the
  HLD's "p50 with bootstrap CI" intent — a defensible nonparametric CI — without
  duplicate code. Glass summaries suppress p99 (HLD: no p99 claims at n=500).
- **Bindings**: `objc2-screen-capture-kit`/`objc2-core-media`/`objc2-core-video`
  (objc2 0.6 already in tree via winit) for capture; servo `core-graphics`
  (already in tree) for CGEvent injection. All macOS-gated in the root manifest,
  per the HLD's file-impact plan; `check-windows.sh` must stay green.
- **Timebase**: injection stamped with `CMClockGetTime(host clock)`; frame times
  from `CMSampleBuffer` presentation timestamps — same clock, so subtraction is
  legal. Startup sanity check: one captured frame's PTS within 1 s of now.
- **Workload geometry**: trials alternate a press key ('x') and an erase key
  (delete) so the watched terminal cell toggles and never drifts. Pre-trial
  quiet gate (300 ms) detects caret-blink pollution; poisoned trials are
  recorded and excluded, not silently dropped.
- Implementation delegated to an Opus worker against a fixed contract; lead
  owns review, live validation (TCC permissions, oracle checks), and the
  measurement runs.

## Log

- Worktree `task/20260817-TSK-HUM-p0-glass-to-glass-latency-probe` off
  origin/main (6a2db95, the signed-off HLD).
- Quench RTT baseline batch 1 (n=1000, 50 ms interval, trunk `probe rtt`):
  **p50 1.49 ms** (95% CI 1.475–1.503), p95 4.09 ms, p99 6.07 ms, max 8.08 ms,
  0 failures. quench resolves to 192.0.2.240:3389 — a faster segment than
  temper (3.31 ms). Provisional until more batches widen the time-of-day span.
- **quench has no SSH service** (TCP 22 times out). P1 rows 2 and 3
  (`wezterm connect` mux, ssh control) need one; enabling Windows OpenSSH via
  the sudo-through-mdrdp route is a P1 prerequisite, not a P0 concern.
- Implementation landed (bc2f238): worker-built against a fixed contract, 23
  new unit tests each proven red once. Lead added one design change: the
  uncertainty band follows the *wider* of configured interval and observed
  median frame gap (SCK asked for 120 fps delivered ~60 in practice).

## Live validation

- **Local Terminal (macOS)**: 20/20 valid at 120 fps capture, p50 24.9 ms;
  20/20 at 60 fps, p50 25.2 ms — capture-rate stability holds (CIs overlap).
- **Focus-routed injection failed on a busy desktop**: with four mdrdp
  processes and other apps stealing frontmost mid-run, 25/30 trials timed out
  because keystrokes went to the wrong app (only ~0 of the probe's events
  reached the session; the session's 33 internal latency samples match the
  setup script's own keys, which bypass macOS focus). Fix: `--target-pid`
  posts via `CGEventPostToPid` straight to the session process — misrouting
  is now impossible. Verified by a single-press trial: the `x` visibly echoed
  at the remote prompt (screenshot), 70.9 ms first / 87.6 ms converged.
- **mdrdp → quench (AVC444v2, 1280x800, DWMFI=15)**: 150/150 valid, 0
  poisoned, 0 timed out. **first-change p50 54.8 ms** (95% CI 51.7–58.0),
  p95 71.7; **converged p50 73.6 ms** (CI 70.4–75.7). Observed capture gap
  16.7 ms (60 Hz delivery — matches the host's 60 fps ceiling), band +8.3 ms
  mean. Sits exactly in the HLD's predicted 45–55 ms glass range.
- Second capture rate (60 fps cap, n=100, same session): **p50 53.0 ms**
  (CI 51.3–60.2) — median stable across rates on the remote target too. This
  run also caught a real host-side stall: trials ~52–85 contiguously timed
  out or ran 0.4–1.5 s (episode began ~8 min into the session, partial
  recovery after ~15 s). Not an instrument artifact — the pattern is a
  contiguous block, medians before/after agree with the 120 fps run. The
  instrument can see latency-degradation episodes, which is requirement 3
  made measurable.
- Remote Windows Terminal caret does not blink over RDP: quiet gates passed
  at exactly 300 ms throughout; 0 poisoned trials in every quench run.

## Oracle validation (P0 acceptance criterion 1) — PASSED

Same 900 s session, both instruments independent:

- internal RTT (down→paint) p50 **35.3 ms**, n=312, stable all session
  (baseline window 36.1) — matches the documented ~34 ms typing anchor.
- glass first-change p50 **54.8 ms** → glass − internal = **19.5 ms**.
- prediction: present p50 2.7 ms (n=4008, this session) + ~half a 120 Hz
  compositor refresh (4.2 ms) + probe sampling bias at the observed 60 Hz
  delivery (+8.3 ms mean) ≈ 15.2 ms, up to ~23.5 with one extra compositor
  frame. 19.5 sits inside the window.
- stable across two capture rates: 54.8 (120 fps capture) vs 53.0 (60 fps),
  CIs overlap; same held locally (24.9 vs 25.2).

Artifacts in `baseline/`: four glass JSONL runs (local + mdrdp target, two
capture rates each) and the session's redacted metrics report. Conditions in
every file header. Note for P1 sessions: start the setup script with `cls` —
timed-out press trials can leave stray `x` glyphs at the remote prompt.

## P1 started, then blocked at the environment

- Quench RTT baseline batch 2 (n=1000, ~55 min after batch 1): p50 1.40 ms
  (CI 1.390–1.415), p95 1.65, p99 1.78, 0 failures. Two batches on record;
  provisional until the time-of-day span meets the baseline rule.
- A row-1 anchor session (n=500 planned) got as far as its setup script —
  then the **Mac locked**: the display idle timer (10 min) ran out between
  probe runs (injected CGEvents reset it during a run; the gaps don't) and
  the lock screen followed. ScreenCaptureKit exposes zero displays while
  locked, so every glass row is blocked until the Mac is unlocked. The probe
  now holds a `PreventUserIdleDisplaySleep` assertion for the whole run
  (`glass/wakelock.rs`) so this cannot recur *within* a run; the lock itself
  needs Arthur.
- The host-description capture rode the same session's script; its output
  was cleared by the script's own trailing `cls` before the lock was noticed.
  Re-capture it in the next attended session (script kept in scratchpad).
- Enabling OpenSSH on quench (needed for P1 rows 2–3 and the P2 SSH tunnel)
  was prepared as a layout-safe elevated input script, but launching an
  elevated host reconfiguration blind — with no way to see the screen — was
  blocked by the permission layer. Deferred to an attended session; the
  layout gotcha for it is recorded: `|` and `~` cannot be typed through the
  US→UK scancode map, so tildes are built with `[char]126`.
