# Rhydra latest-state flow control journal

## 2026-08-25 — intent and evidence

Goal: make window drag and other interactive visual changes remain current under backpressure, without weakening H.264 or move ordering.

The current depth-two video and sparse queues reject the newest item when full. That bounds memory but preserves old work, so it does not bound visual age. The server also cannot infer client canvas progress from queue admission or TCP delivery.

The client has exact logical completion points after canvas updates. Moves are provisional until their declared remainder completes. Regional sparse and video updates may complete out of sequence, so feedback must name exact logical frame sequences rather than advertise a cumulative maximum.

IDD already selects the newest valid unread slot and accumulates dirty coverage across skipped presents. Therefore, stopping before `capture.acquire()` while two client-applied permits are outstanding gives latest-state sampling without inventing a replacement queue or discarding encoded reference frames.

The design uses strict wire-v10 acknowledgements on the reverse direction of the video socket, a two-update server window, and epoch-guarded reconnect state. The input connection stays independent.

Design review and implementation evidence will be appended below.

## 2026-08-25 — adversarial design review

Three reviewers independently checked correctness, concurrency, and latency, then cross-refuted every other finding. The important correction was mixed-update completion: one logical sequence may have raw and video legs on separate sockets, so wire v10 must carry a required-part mask and the client must acknowledge only after both legs finish. Other surviving findings tightened suppressed/failure semantics, move completion, reader ownership, epoch atomicity, encoder pumping during waits, nonblocking feedback enqueue, recovery after post-acquire rejection, and measurable avoided work. The HLD now contains those constraints.

## 2026-08-25 — external design review

Claude/Fable completed the required one-pass review. It approved implementation after calling out two material gaps: cancellation needs an RAII reservation token, and a post-wait IDD acquisition loses ordered move metadata. The design now makes cancellation structural, requires H.264 low-latency mode, covers tiled updates and precise timeout age, and instruments post-wait move-versus-pixel routing. Arbitrary skipped-move composition was rejected because the three-slot IDD pool cannot retain an unbounded chain back to the client's baseline; accumulated final pixels remain the safe fallback and a live Quench run will quantify its cost.

## 2026-08-25 — implementation

Wire v10 now carries exact frame acknowledgements and required raw/video part masks. The client completes one logical sequence only after every declared canvas part succeeds, including move remainders and tiled video, and sends the exact sequence through a bounded writer on the reverse video socket. Suppressed but validated regions complete; malformed, failed, inconsistent, or feedback-blocked sessions stop rather than claiming progress.

The server now holds at most two epoch-scoped visual reservations. It pumps ready encodes before the gate, waits in one-millisecond condition-variable slices without acquiring another IDD frame, and resumes from IDD's newest valid state. Reservations remain tentative until queue admission and cancel automatically on abandoned paths. Admitted H.264 dependencies are never replaced. A six-second oldest-reservation deadline shuts down only that viewer socket and returns the sender to accept.

Telemetry schema 13 records the configured window, current and maximum occupancy, ACKs, wait count/time, avoided captures, post-wait gaps, move/pixel routing, and the oldest reservation phase.

## 2026-08-25 — code review and simplicity

The independent review found no critical defect. It identified four high-severity gaps, all fixed: mandatory H.264 low-latency control, stale-epoch handling that does not unwind the server, successful completion for fully suppressed move remainders, and bounded duplicate histories. It also prompted immediate timeout socket shutdown. The server keeps a six-second deadline, deliberately beyond the client's existing five-second move failure deadline.

The simplicity pass removed test-only convenience aliases from `VisualFlow`, kept one admission verb and one capture-skip counter entry point, and reduced cancellation to `Result<()>`. No new abstraction or dependency was added.

## 2026-08-25 — validation

Both bounded-history regressions were observed failing with a deliberately raised 17-entry implementation limit against the required 16, then passing after restoration. Focused native-session tests passed (57). The full client suite passed (868). Portable server tests passed (344). Root formatting and clippy with warnings denied passed. The Windows cross-check passed for the client and Rhydra server; it retains two pre-existing `src/present.rs` unused-parameter warnings and the known absent cross-build icon warning. Portable server clippy passed with warnings denied after allowing the pre-existing `manual_is_multiple_of` lint class in untouched expressions. No IDD source changed, so its separate driver build was not applicable. Hardware-backed Quench validation remains post-integration by policy.
