# Microphone input

- Started a separate task worktree from `origin/main` for the MS-RDPEAI channel approved in the self-approved HLD.
- Confirmed IronRDP 0.17 exposes DVC listener and framing APIs but no audio-input processor. The existing `cpal` dependency provides capture.
- Added a focused PCM format and packet-bound test, observed it fail before implementing the parser, then observed it pass.
- Built the `AUDIO_INPUT` DVC with background `cpal` capture, bounded command/raw/output queues, PCM16 packetization, and generation cancellation on close. The session thread remains the only RDP socket writer.
- Code review found an unbounded control queue and a possible OPEN reply delay during microphone permission/startup. The command queue is now bounded and nonblocking. A ticket gives the session pump a three-second `E_FAIL` deadline, ahead of Windows' five-second limit; late capture is cancelled. Focused tests for timeout, queue capacity, and callback capacity were observed red before their fixes, then green.
- Re-ran all eight microphone tests and clippy cleanly. The worker also ran the repository's Windows cross-check successfully. Live audio and macOS microphone permission remain unverified because the unattended Quench smoke stalled in credential lookup before opening an RDP session.
- Rebasing onto integrated clipboard and keyboard was conflict-free. The combined focused run exposed an intermittent clipboard staging test failure: 32 immediate lock retries could exhaust while another test briefly held the shared lock. Changed it to a bounded 250 ms wait; a new contention test failed against a zero-wait variant, then passed. The combined clipboard (54), microphone (8), session (99), window (72), settings (7), and Settings UI (12) tests passed, as did all-targets clippy, fmt, and both Windows cross-check targets. The existing Windows icon warning remains.
