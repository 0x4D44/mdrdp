# 2026.08.17 — P3: IDD driver spike (low-latency exploration)

Executes P3 of the signed-off HLD (`wrk_docs/2026.08.17 - HLD - low-latency
server exploration.md`), **pulled forward past Gates A/B on Arthur's explicit
direction** (2026-08-17: "actually building the thing is going to be the most
instructive thing we can do"; P1 rows and the quench SSH/wezterm setup parked).
Gate A/B measurements still happen later; they are no longer sequenced before
this build.

## Design decisions at build start

- **Derive from Microsoft IddSampleDriver** (MIT,
  microsoft/Windows-driver-samples@717778a2), per the HLD. The sample is 809
  lines and structurally complete: WDF plumbing, IddCx adapter/monitor
  lifecycle, and a swapchain processor thread.
- **One EDID-less monitor.** The sample fully supports EDID-less monitors via
  `MonitorGetDefaultModes` + `QueryTargetModes` — both driver-controlled — so
  240 Hz needs no EDID authoring at all. Modes: 1920x1080@240 (preferred),
  @120, @60; target modes add 2560x1440@{120,60}.
- **IddCx 1.6 headers, minimum-required 1.4** (the sample's own pairing);
  UMDF 2.25; `UmdfExtensions = IddCx0102` (the extension contract id, not a
  header version).
- **WPP tracing stripped**; instead a per-600-frames cadence line via
  `OutputDebugStringW` — enough to verify 240 Hz swapchain delivery with
  DebugView and nothing else.
- **Swapchain loop stays the sample's acquire-and-release null consumer.**
  DDA measures against the virtual display for stage 2; the IddCx
  swapchain-as-capture-path comparison is a later increment on this base.
- **Cross-build from macOS** rather than provisioning MSVC+WDK on quench:
  Apple clang's `--driver-mode=cl -target x86_64-pc-windows-msvc` emits valid
  amd64 COFF (verified), rustup's `rust-lld -flavor link` is lld-link, the
  repo's xwin splat provides CRT+SDK 10.0.26100, and the WDK arrives as a
  plain NuGet zip (Microsoft.Windows.WDK.x64 10.0.26100.6584, ~110 MB,
  splatted to `~/.xwin/wdk`) — it even contains `Inf2Cat.exe` for the
  quench-side packaging step. Zero toolchain installs on either machine.
- Driver derivation + build-script grind delegated to an Opus worker against
  a fixed contract; lead owns review, the deploy runbook decisions, and the
  quench deployment (parked until attended).

## Log

- Worktree `task/20260817-TSK-HUM-p3-idd-driver-spike` off origin/main
  (5fb79ad, v0.1.54 — includes the P0 glass probe this spike will be
  measured with).
- Sample sources sparse-cloned; WDK NuGet fetched and splatted.
- **First Windows binary linked from this repo's toolchain**: a hello-world
  `cargo build --target x86_64-pc-windows-msvc` with `-C linker=rust-lld` and
  three xwin `/libpath`s produced a valid PE32+ console exe on the first try.
  The HLD's "first Windows link" prerequisite is discharged in principle —
  the spike server's real dependency set (windows-rs D3D11/MF) still has to
  prove out, but the link path exists with zero new tooling.
- P2 spike-server scaffolding started in parallel (disjoint directory from
  the driver work): stage 2 re-runs the stage-1 measurement on the virtual
  display, so the server is on this spike's own critical path.
- **Driver landed (43670c3)**: `tools/latency-spike/idd/` cross-builds clean
  from macOS — mdrdp_idd.dll (exports exactly `FxDriverEntryUm`) and the
  SwDevice creator exe. Lead re-ran the build from clean and reviewed the
  full derivation. Notable toolchain facts recorded in build.sh: `ntdll.lib`
  needed for the UMDF stub's `DbgPrintEx` (OneCoreUAP does not resolve it);
  SDK/WDK headers cross-include with inconsistent case, so the build relies
  on a case-insensitive volume.
- `deploy.ps1` added (a906a0a): two-phase attended deployment (test signing +
  reboot, then cert/Inf2Cat/signtool/pnputil), teardown mirrored. The
  attended quench session is now minutes, not an evening.
- Spike-server worker lost twice mid-flight to provider 529 overload errors;
  portable protocol modules (framing, Annex B parameter-set handling, input
  records, stats shapes, a hand-packed D3D11 colour-space bitfield) are
  written and lead-reviewed — resumed the worker for the Windows pipeline
  half after a backoff.
- **Server landed (39d6726)**: cross-linked spike-server.exe, the repo's
  first real Windows executable. 46 native tests; both clippy targets clean;
  full MF/COM import surface resolved from the xwin splat. Design points
  worth remembering: send queue bounded at two and lossy with counted drops
  (a queued frame is a stale frame); capture does not run without a client;
  SPS/PPS guaranteed belt-and-braces with the route recorded per frame.
  Nothing has executed on Windows — the async MFT event pump is the named
  first-run risk.
- Viewer chunk in flight: mdrdp's own decode + `window::present_into` +
  softbuffer (client held constant), forwarding local keyboard events as
  input records so `probe glass --target-pid` measures the spike path
  identically to row 1.
- **Viewer landed (b52e0f2)** — the kit is complete: driver, server, viewer,
  all built from this Mac. The catch that mattered: `[patch.crates-io]` does
  not apply from inside a standalone workspace, so the viewer repeats
  mdrdp's six vendored IronRDP patches with rebased paths — without that it
  would have linked a *different* client than the one it holds constant.
  Client stats are stamped on the CoreMedia host clock, the same clock
  `probe glass` uses, so the two files overlay without conversion. Live
  smoke showed VideoToolbox live and refusing a garbage AU with mdrdp's own
  error text.
- Root workspace untouched by this branch (tools/ crates are standalone
  workspaces); src/ is byte-identical to origin/main. First frame from a
  real encoder, the MFT pump, and everything else Windows-side runs in the
  attended session per the checklist above.
- Quench RTT baseline stands at n=3000 over 3.0 h (p50 1.415 ms, CI
  1.405–1.422); one more batch after ~17:50 local settles the ≥6 h span
  rule.

## Attended-session checklist (everything that waits for Arthur)

In order, one sitting, ~30-45 min of his presence:

1. **Enable OpenSSH on quench** (elevated, 3 commands — or the prepared
   layout-safe input script under supervision). Everything downstream ships
   bits and tunnels through it; it also unblocks parked P1 rows 2-3.
2. **Deploy the driver**: copy `idd/build/` + `deploy.ps1`; `-Phase pre`
   (reboot), `-Phase install`, run `mdrdp-idd-create.exe --wait`. Verify the
   240/120/60 modes and DebugView's `mdrdp-idd:` cadence lines.
3. **Spike stage 1** (real display, ~60 Hz cadence): run `spike-server
   --list-outputs`, pick the console output, tunnel 9500/9501, run
   `spike-viewer`, shake out the MFT pump, then `probe glass
   --target-pid <viewer>` for the first decomposed budget.
4. **Stage 2**: same against the virtual display output at 240 Hz (and 120).
5. Quench host description re-capture (script in scratchpad) and RTT batches
   to settle the baseline span, if not already done.
