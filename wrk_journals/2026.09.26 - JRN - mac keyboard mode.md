# Mac keyboard mode

- Started a separate worktree from `origin/main` for the opt-in RDP keyboard mode. The existing positional mode stays the default.
- The implementation uses a physical-key ledger to pair each Unicode or scancode press with its release. Review checked the possible overlap between ordinary key text and IME commits.
- Reviewed winit 0.30.13's macOS event source: committed marked text suppresses the corresponding key press, leaving `Ime::Commit` as its owner. Focus loss releases ledger-held Unicode units.
- Independent code review found no correctness blocker. Exact-tree clippy found an overlong helper signature and derivable default; the helper was narrowed to Mac mode and the default derived. The worker observed the new test compile red before implementation and then all focused suites green. A real layout/IME session check is still pending.
- To prove the focused test exercised the translator, temporarily changed its expected `é` unit to `x`; the test failed with actual 233 versus expected 120. Restored the assertion and observed it pass. Re-ran the 55 input tests and clippy cleanly on the restored tree.
- A bounded live Quench smoke stopped before the `connecting to` marker, in credential lookup or its OS prompt. A second bounded `--stage-json` attempt reached the same point. Neither established a session; no wire or layout behavior was observed. Do not count this as live validation. Further unattended attempts were stopped.
