# JRN — Rhydra function, latency, and rendering hardening

## 2026-08-23 — intake and baseline

- Goal: match Windows App's everyday function and rendering quality while beating its
  input-to-visible latency at 5K/200% on Quench.
- Started a dedicated docs worktree from `origin/main` at `4b03e518`.
- Claimed Quench on the fleet board. No current claim was visible.
- Non-disruptive deploy probe: Rhydra v0.5.0 is healthy; Quench console user is `marti`;
  desired display is 5120×2880 at 240 Hz and 200%; driver and VB-CABLE checks are green;
  dry-run planned no mutation.
- Local client is `mdrdp v0.1.125`. The installed comparator is Windows App 11.3.8 build
  3054 (`com.microsoft.rdc.macos`).
- Confirmed `MDR-BUG-FLU-00034`: `mdrdp Quench --doctor` fails key authentication, while
  the explicit `quench.lan.example --user marti --ssh-user marti --doctor` path is green
  through SSH, tunnel, agent, display, server, input desktop, audio, and empty viewer slot.
- Opened independent function, latency, and rendering review lanes. No reviewer may edit,
  open a viewer, or touch Quench.
- Drafted the hardening HLD. The previous low-latency design's omitted Windows App row is
  superseded by Arthur's explicit comparison requirement.
- Attempted the single read-only Claude/Fable review with Claude Code 2.1.240. It returned
  a session-limit refusal before reviewing the design, so the external review did not run
  and was not retried. Correlated same-family review blind spots remain a residual risk.

## Review and implementation log

- Function review found disconnect-held input and a bounded-deadline defect. It refuted
  auto-reconnect, resize, and visibility items as intentional MVP exclusions.
- Latency review found two production server risks (stats on the sender thread and
  message-sized 5K backpressure), one production telemetry bias, one reader batch risk,
  and three spike-viewer-only costs.
- Rendering review found the IOSurface pool falls back to overwriting a surface that
  CoreAnimation still reports in use. It refuted current channel-order, move-rect,
  AVC444, H.264 reference, and HEVC epoch hypotheses.
- All three reviewers completed one cross-lane refutation pass. Every candidate mechanism
  survived, but deadline, queue visibility, stats-thread, and batch/UI overclaims were
  narrowed in the HLD.
- Raised `MDR-BUG-FLU-00048` through `MDR-BUG-FLU-00057` for the ten new survivors.
- Marked the reviewed design `SELF-APPROVED`. External Fable review remains the stated
  residual risk; there is no unresolved architectural fork at this checkpoint.

## Green unit — bounded Windows clipboard reads

- Claimed `MDR-BUG-FLU-00042` in a fresh worktree.
- Added a portable malformed-allocation oracle and observed it fail before the Win32 fix.
- The Win32 reader now uses `GlobalSize`, rejects zero/odd/unterminated data, and unlocks
  through an RAII guard on every successful lock path.
- Focused clipboard tests: 29 passed. Windows repository check passed. Independent review
  found no required changes; two optional expansions were rejected as unexecuted
  Windows-only coverage and separate resource-policy scope. Simplicity pass made no change.

## Green unit — release held input on disconnect

- Claimed `MDR-BUG-FLU-00048` in a fresh worktree.
- Added per-connection tracking for successfully injected VK, scancode, and mouse-button
  transitions. Every connection exit now attempts the matching releases before accepting a
  successor; motion and wheel records are never tracked.
- The new release-plan oracle was observed red with two assertion failures, then passed
  2/2. The full Rhydra suite passed 310 tests and the Windows repository check passed.
- Code review disposition:

  | # | item | type | action | note |
  |---|---|---|---|---|
  | 1 | surface failed disconnect releases | Issue | Disagreed | failure is already logged; returning an error would not retry or prevent recovery accept |
  | 2 | preserve pending move telemetry on I/O error | Issue | Fixed | made the final flush unconditional, which also simplified exit handling |
  | 3 | add a full fake-Win32 exit-path harness | Optional | Skipped | would add a broad abstraction beyond the pure ledger oracle and Windows compile gate |
  | 4 | keep bookkeeping out of the external crate API | Nit | Fixed | module is now crate-private and absent from non-host non-test builds |

  Two review changes applied, one optional skipped, one issue disagreed with. The final
  Windows check introduced no new source warnings.

## Green unit — remove stats persistence from the per-message sender path

- Claimed `MDR-BUG-FLU-00050` in a fresh worktree.
- The server now buffers stats-file rows and flushes every 200 ms while making progress
  plus at shutdown. Each bounded sender batch delivers rects and frames before telemetry,
  with stable order inside each class. No thread, queue, dependency, or flag was added.
- The two scheduling/flush tests were observed red, then passed. Full Rhydra suite passed
  312 tests and the Windows repository check passed.
- Code review disposition:

  | # | item | type | action | note |
  |---|---|---|---|---|
  | 1 | failed writes still receive `send_done_us` | Issue | Disagreed here | exact open bug `MDR-BUG-FLU-00045`; preserve one-bug scope |
  | 2 | slow socket defeats a hard 200 ms flush bound | Issue | Clarified | interval is bounded only while this sole sender makes progress; a hard bound needs another thread |
  | 3 | payload-first changes cross-class JSONL order | Optional | Clarified | timestamp and `frame_seq` are the documented causal keys |
  | 4 | several hot-path batch allocations | Nit | Fixed | stable in-place batch sort and reusable stats-line vector |
  | 5 | add a full socket/file integration harness | Optional | Skipped | pure regressions plus full server/Windows gates cover this narrow scheduler change |

  One review cleanup applied, two items clarified, one separate bug rejected from scope,
  and one optional harness skipped. Final focused and Windows checks remained green.
