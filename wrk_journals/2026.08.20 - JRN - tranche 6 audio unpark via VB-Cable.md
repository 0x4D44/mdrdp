# JRN — tranche 6 audio unpark via VB-Cable

## Goal

Make real Windows desktop audio audible through mdrdp by giving each rhydra host a
VB-CABLE render endpoint, hardening the existing WASAPI loopback path, adding deploy
and doctor support, and proving the result on quench before temper.

## Scope

- In: VB-CABLE provisioning, endpoint detection and format pinning, loopback capture
  lifecycle, default-device policy, doctor guidance, real two-tone evidence, and soak.
- Out: a custom audio driver, RDP protocol-provider work, per-session isolation, Opus,
  and wire-format changes.

## Live state

- Worktree: `~/worktrees/mdrdp/20260820-TSK-HUM-tranche-6-audio-vb-cable`
- Branch: `task/20260820-TSK-HUM-tranche-6-audio-vb-cable`
- Status: design SELF-APPROVED after native, cross-family, and targeted critical-fix
  review.
- Next: commit this checkpoint and implement without waiting.

## Evidence and decisions

- Arthur selected the base VB-CABLE product. Premium cables and a custom driver are
  outside this tranche.
- The landed transport already carries PCM16 audio over the auxiliary channel and has
  a non-gating `audio` health rung.
- The existing Windows loopback implementation was written against the WASAPI contract
  but has not yet run against a live render endpoint.
- The official Pack45 archive downloaded on 2026-08-20 is 1,318,877 bytes with SHA-256
  `b950e39f01af1d04ea623c8f6d8eb9b6ea5c477c637295fabf20631c85116bfb`.
- The base package contains `vbMmeCable64_win10.inf`, its catalog/sys pair, and the x64
  setup program. Its readme requires an administrator install and a reboot to finalise.
- Quench is currently claimed by another fleet task. No mutating host work will start
  until that owner releases it.
- Three independent native reviewers completed critique and mutual refutation. No
  critical issue survived. The HLD now installs before agent quiesce, uses stable PnP
  identity and a crash-safe default-device lease, routes policy through the interactive
  agent, constructs capture lazily, bounds packet/ring behavior, and gives live claims
  numerical oracles.
- Rejected review items: a multi-client reference counter conflicts with the existing
  serialized-session invariant; the red-before-green rule was already explicit.
- The one-pass Claude/Fable review found two critical design errors. `pnputil` cannot
  create VB-CABLE's root device, and a shared 200 ms trim would cut through normal
  RDPSND lead. The HLD now uses the official silent setup and scopes trimming to the
  native ring. It also narrows underrun/gap oracles to continuous render windows,
  preserves the existing source setting, adds latency measurement, strengthens the
  signature bar, defines the video workload, and clarifies loss restoration.
- The single targeted native re-review passed both critical corrections. It confirmed
  the official setup resolves root-device creation while preserving pre-quiesce safety,
  and that native-only ring construction can leave RDPSND behavior unchanged.

## Validation log

- Quench package verification, before installation: archive length 1,318,877 and SHA-256
  `b950e39f01af1d04ea623c8f6d8eb9b6ea5c477c637295fabf20631c85116bfb` matched.
  `VBCABLE_Setup_x64.exe` and the x64 `.sys` had valid Burel Vincent EV signatures;
  the x64 catalog had a valid Microsoft Windows Hardware Compatibility Publisher
  signature. The INF's provider token resolved to `VB-Audio Software` and hardware ID
  `VBAudioVACWDM`.
- Quench official setup: `VBCABLE_Setup_x64.exe -i -h` exited 0. No reboot ran. It
  created healthy `ROOT\MEDIA\0000`, two active render endpoints, one active capture
  endpoint, and a viable 48 kHz/stereo WASAPI loopback client. The pre-existing mdrdp
  agent was not stopped or replaced. Standard CBS and Windows Update reboot-pending
  keys were both absent after install; the last boot remained 2026-08-18 16:20:15.
- Pack45's observed friendly names differ from the older plan: `Speakers (VB-Audio
  Virtual Cable)` and `CABLE In 16 Ch (VB-Audio Virtual Cable)` are render endpoints;
  `CABLE Output (VB-Audio Virtual Cable)` is capture. Stable PnP/provider identity is
  therefore an acceptance oracle; literal friendly names are evidence only.
- Each endpoint carries generic Microsoft endpoint properties but points through
  `DEVPKEY_Device_Parent` to `ROOT\MEDIA\0000`. That parent supplies the stable facts:
  hardware ID `VBAudioVACWDM`, service `VBAudioVACMME`, manufacturer and driver provider
  `VB-Audio Software`. Endpoint instance IDs encode render as `{0.0.0...}` and capture
  as `{0.0.1...}`. Both implementation workers received these measured values.
- The unique remote and local installer staging directories were removed after use.
- P0 interactive-render check: a temporary `/it /rl highest` scheduled task ran in
  quench console session 2. PowerShell generated and synchronously played a five-second
  48 kHz/16-bit/stereo WAV with 440 Hz left and 660 Hz right; `SoundPlayer.PlaySync`
  completed successfully and reported a 960,044-byte file. The task and all local and
  remote scratch were removed. This proves an ordinary console app can render; it does
  not yet prove mdrdp capture or Mac playback.
- The native telemetry worker observed every new regression red before its fix, then
  passed 722 root library tests. A lead review removed an arrival-time cutoff that hid
  real capture-position gaps, made forward and overlapping discontinuities count, and
  kept the newest audio when the native-only 200 ms high-water mark is crossed.
- A follow-up simplicity pass removed blocking telemetry work from the CPAL callback:
  current depth is atomic, and the fixed histogram uses one non-blocking tracker lock.
  All 42 focused audio tests pass after that change.
- Sunshine's current Windows source confirms that its device-format call uses a
  48 kHz/stereo/16-bit `WAVEFORMATEXTENSIBLE` PCM shape plus a non-null zeroed second
  format pointer. The Windows policy implementation must match that contract and read
  the format back before it marks provisioning healthy.
- A live property sweep on quench found the stable role discriminator that MMDevice's
  generic `DeviceDesc` lacks: `DEVPKEY_Device_BusReportedDeviceDesc`. Pack45 reports
  `Speakers (VB-Audio Virtual Cable)` for the base render pin and `CABLE Output
  (VB-Audio Virtual Cable)` for capture. The implementation combines that driver-set
  role with the verified parent PnP identity; user-renamable friendly names remain
  evidence only. The temporary probe was removed locally and from quench.
- Lead review corrected the Windows slice before validation: exact extensible PCM
  format plus read-back, a non-null default-format argument, missing-default lease
  handling, current-default fallback when CABLE policy is unavailable, elevated-side
  marker/source writes, full-deploy source propagation, consecutive WASAPI packet
  draining, wire-sized splitting with device positions, and separate produced,
  queued, socket-written, silent, and outbox-dropped counters.
- New quiet-window continuity coverage was observed red before implementation, then
  green. New socket-written telemetry coverage was also observed red before the writer
  report existed, then green. WASAPI split/coalescing coverage failed the Windows
  compile before `queue_pcm` existed, then the Windows check passed after implementation.
- Portable server tests pass 293/293. Root library tests pass 722/722. Deploy-focused
  tests pass 21/21. `scripts/check-windows.sh` and the standalone audio-probe Windows
  target check pass; the icon warning is the documented non-Windows cross-build shape.
- The independent implementation review found one critical deploy blocker and three
  correctness gaps. The official installer postcheck filtered a nonexistent `kind` field
  instead of the emitted `flow`; its focused test failed before the correction and now
  passes. Configure now assigns CABLE only to default roles that were genuinely empty.
  Preflight calls the interactive agent's read-only `check-audio` path and requires live
  format readback, so a stale marker cannot report health. Client continuity no longer
  treats a 100 ms network delay as expected silence: the host emits one zero-PCM boundary
  on entry to quiet and the client resets only on that boundary.
- The missing-default and explicit-boundary regressions both failed before their helpers
  existed, then passed after implementation. The stale-marker preflight assertion also
  failed before the live check was added. Portable server tests remain 293/293 and the
  Windows cross-check passes after all four fixes. The single permitted targeted
  implementation re-review then confirmed all four prior findings resolved and found no
  critical defect introduced by the corrections. It independently passed 17 deploy tests,
  the focused continuity regression, 293 server tests, and `git diff --check`.
- The quality-only simplicity pass removed the unsafe JSON substring check, kept live
  preflight read-only, made the configure command wait through the scheduled-task startup
  race, and ensured the zero-PCM boundary never reaches the playback ring. Final local
  checks before the live build: root tests 722/722, server tests 293/293, audio-probe host
  check, root and server clippy with warnings denied, Windows cross-check, rustfmt, and
  `git diff --check` all pass.
- The first quench deploy dry-run stopped before mutation because the old schema-5 agent
  wrote its expected unknown-`check-audio` refusal to stderr. With PowerShell's stop policy,
  direct native invocation promoted that stderr into a terminating `NativeCommandError`
  instead of a false preflight fact. A focused assertion failed first; preflight now uses
  `Start-Process -Wait -PassThru` and classifies the child exit code without interpreting
  its stderr. The focused deploy test and diff check pass before the dry-run retry.
- The next dry-run exposed a second conservative-probe defect: quench had only Edge update
  cleanup paths in `PendingFileRenameOperations`, but the probe labelled any machine-wide
  pending rename as an audio reboot. The focused assertion failed first. Reboot evidence is
  now attributed to VB-CABLE only when setup ran and its verified render or capture endpoint
  is still unavailable. The retry reports `reboot=false` with both endpoints healthy.
- Live deployment is now blocked by session ownership, not code. `quser` shows the documented
  `ano` session 2 disconnected while an unexpected `marti` session 3 is active. The old
  rhydra agent and IDD creator remain in session 2. Deploy correctly refuses to replace them
  because `Win32_ComputerSystem.UserName` has no matching interactive `ano` console user.
  Kicking or logging off `marti` would discard another user's active session and was not
  attempted. Arthur must confirm when quench is free or authorize that session transition.
