# HLD — rhydra tranche 5: clipboard, text-first, both directions

Step 5 of the parent HLD's §9 sequencing (§6 there). Tranche 4 landed the health
ladder (v0.1.77, agent 0.4.0, control schema 3).

This is **product requirement #1** — "clipboard dies mid-session; ours must
survive an all-day soak with zero wedges".

---

## 0. The fork, and how it was resolved

Two independent adversarial reviews found the same blocker, and I verified it
against the code:

> **The native transport had no client→server message channel.**

- The **video socket is server→client only**. `win/send.rs`'s `poll_client_eof`
  *peeks* and never consumes — "the viewer never sends on this socket". Client
  bytes there would accumulate forever *and* break the EOF detection that frees
  the single viewer slot.
- The **input channel cannot carry it**. `input_proto` is frameless and
  fixed-width per kind: an unknown kind "cannot be skipped — it is terminal for
  the connection". `win/input.rs:474` closes it; `src/native/session.rs` reads
  that as "the host closed the input channel" and tears the session down.

So the parent HLD §6's premise held **server→client only**. Client→server, a
stray clipboard message to a 0.4.0 host would have killed keyboard, mouse and
the session.

**Arthur's decision (2026-08-19): option C — a dedicated auxiliary channel, and
the same for audio.** In his words: video latency is critical; audio, and even
more so clipboard, are lower-priority traffic and must not back up the video.

That is a stronger principle than "clipboard needs a channel", and it is now a
settled input for the rest of the build-out. The parent HLD §6 has been amended
to record both the correction and the principle.

### What the principle decides, beyond this tranche

**Three priority tiers, and traffic never crosses downward into a higher one:**

| Tier | Traffic | Channel |
|---|---|---|
| 1 — critical | video | 9500, untouched by this tranche |
| 2 — continuous, latency-sensitive | audio (tranche 6) | 9503, ranked first |
| 3 — bursty bulk | clipboard | 9503, ranked last |

Audio and clipboard share the auxiliary socket rather than taking a port each,
with a **strict-priority sender: any pending audio goes before any pending
clipboard**. The alternative — a fifth port — buys isolation between tiers 2 and
3 that a priority sender already provides, at the cost of another forward and
another probe step.

The one way sharing could still hurt audio is head-of-line blocking *within* a
message: a large clipboard payload occupies the stream while audio waits. That
is bounded by the §7 text ceiling (≤256 KiB, a few ms on this LAN), and it is
the number tranche 6 must measure rather than assume. **If audio jitter traces
to clipboard, splitting tier 3 onto its own port is a small additive change** —
recorded here so the option is not lost.

## 1. The one-sentence deliverable

Text on the Mac's pasteboard appears on quench's clipboard and vice versa, over
the native transport, with no state that can fail to return to idle — and with
soak evidence, because a green unit test does not discharge a soak-class
requirement.

## 2. What push-only does and does not buy — corrected

The first draft claimed the native path is "structurally wedge-proof" because
there is no round trip. **That claim was too strong and is withdrawn.**

What is genuinely removed: CLIPRDR's *pending-paste* wedge — the state where we
have asked for content and may never be answered. mdrdp's RDP bridge spends a
5 s timeout and a state machine escaping it. A push design has no such state.

What is **not** removed, and must be designed for:

- **Transport backpressure.** A socket write blocks when the peer's window
  fills; the ssh tunnel adds another buffer. Server→client shares a **2-deep,
  deliberately lossy** `sync_channel` with video (`win/pipeline.rs`,
  `QUEUE_DEPTH = 2`) behind a single writer with a 5 s write timeout. `try_send`
  drops silently under any video load; `send` blocks the clipboard thread. Both
  violate the invariant. **Resolution:** clipboard gets its own depth-1,
  keep-latest slot the sender checks each lap. Clipboard is last-writer-wins, so
  superseding a queued payload is not a loss, and unbounded growth is impossible
  by construction. A stalled slot drops and reports; it never blocks.
- **The OS clipboard.** See §6 — and note the first draft named the wrong
  hazard.
- **The suppressor itself.** §5's first draft relocated the wedge rather than
  removing it: it could silently and permanently refuse to send a legitimate
  copy. Corrected below.

Head-of-line blocking is the reason the text ceiling (§7) is load-bearing rather
than defensive: a multi-MB clipboard message occupies the reassembler while
video queues behind it, on a product whose premise is latency.

## 3. What is reused — corrected

Reused **unchanged**, because they are about the OS rather than CLIPRDR:

- `OsClipboard` / `ArboardClipboard`, whose handle is dropped and recreated on
  any error so a poisoned handle cannot disable the clipboard for the session.
- `ClipboardPolicy`'s **direction gates**.
- `content_fingerprint` — the bounded-cost **hash function**.
- The ~250 ms local poll.

**Corrections to the first draft's claims:**

- It said `ClipboardPolicy` supplies "the size ceiling". It does not: it has
  `max_image_bytes` only, and images are out of scope. **There is no text
  ceiling anywhere in the codebase.** See §7.
- It called `content_fingerprint` "the echo suppressor". The hash function is
  reused; the **suppression policy is new**, and the first draft specified it
  differently from — and worse than — the working RDP one.

## 4. The auxiliary channel (settled)

**Port 9503, framed, bidirectional, one connection per session**, forwarded by
the same ssh tunnel as the other three. It uses `framing` — the same
length-prefixed encoding as the video socket, so unknown message types skip
safely in *both* directions, which was only ever true one way before.

Chosen because it is the only option that changes no proven code: the
single-writer video path and the frameless input path both stay exactly as they
are, and neither the EOF detection nor the latency-critical input protocol is
touched. It costs one forwarded port and one probe step, both mechanical.

- `ForwardPorts` gains `aux`; `TunnelSpec` forwards it.
- The probe gains a step: connect the aux socket **only when the host advertises
  the capability**, and treat a failure there as "no clipboard", never as a
  session failure. A session without clipboard is a working session.
- Message types on this channel live in their own namespace, deliberately
  disjoint from `input_proto`'s record kinds, so a framing mistake can never be
  read as a valid input record. (A stray `5` on the input channel is a valid
  `MouseMove`; nothing here may share that hazard.)

**The capability flag is a safety gate, not a courtesy.** The client must not
open the aux socket or write a byte upstream unless the host's header advertises
`clipboard: true`. Against a 0.4.0 host there is nothing listening on 9503, and
the flag is what stops the client trying. The header gains `clipboard: bool`
(serde-defaulted false) and `SCHEMA` bumps 5 → 6; **`wire_version` stays 3** —
no forced redeploy for a feature addition.

## 5. Echo suppression — corrected, because the first draft was wrong

The first draft: *"each side records the fingerprint of the content it last
applied from the wire, and never sends content whose fingerprint matches it."*
Both reviewers produced the same counterexample:

1. A applies **X** from the wire (`last_applied = X`).
2. The user on A copies **Y**. A sends Y. B now holds Y. `last_applied` is still
   X — A originated Y, it never applied it.
3. The user on A copies **X** again → fingerprint matches `last_applied` →
   **suppressed**. B still holds Y.

"I copied it and it didn't paste" — silent, permanent, trivially reachable. The
draft's justification ("the two ends already agree") was false: they agreed at
the instant of the apply, and the other end has since moved on.

**The correct model is the one the RDP bridge already implements**: one slot
holding the fingerprint of the last content observed **from either source** —
updated on a local observation *and* on a wire apply (`src/clipboard.rs:519`,
`:780`). Replayed against the counterexample, step 2 sets the slot to Y, so
step 3 sends X. Correct.

Two further corrections:

- **Line endings must be canonical on the wire (LF), converted at exactly one
  place** — CRLF on the Windows `SetClipboardData`, LF on read-back. Without
  this, a multi-line copy ping-pongs forever at poll cadence: the Mac sends
  `a\nb`, Windows stores `a\r\nb`, the listener sees a change, sends it back,
  AppKit normalises to `a\nb`, and round it goes. Fingerprint suppression cannot
  break that loop because the round trip is not byte-identical.

  **Amended during implementation, 2026-08-19.** This section originally added
  "belt and braces: track the last-**sent** fingerprint too". That was wrong and
  is **not implemented**: consulted as a suppression key, a last-sent slot
  reintroduces the exact counterexample this section was written to fix. After
  send-X then apply-Y, a re-copy of X matches the last-sent slot and is refused
  — the same silent permanent loss, reached by the same three actions.

  Two things replace it, and between them the loop is closed properly:

  1. **The fingerprint is taken over the canonical (LF) form**, not the local
     bytes. This is what makes suppression work across the CRLF boundary at all:
     a Mac holding `a\nb` and a Windows box holding `a\r\nb` hold the same
     clipboard, and any scheme hashing local bytes calls them different forever.
     With canonical-form hashing the ping-pong does not start.
  2. **After applying wire content locally, the slot is seeded from what the OS
     actually holds** (`Bridge::note_applied`), never from what was asked for. If
     the pasteboard normalises what it was given, the next poll would otherwise
     read a change and send it, and the peer would answer — forever. Seeding
     from the read-back makes the next poll agree by construction, which the
     last-sent slot could not do.
- **Seed both slots from current content at connect.** The slot starts empty, so
  the first poll reads as a change and one end clobbers the other's clipboard
  with no user action — and which end wins is a race.

**Stated, not solved:** simultaneous copies at both ends within one poll interval
leave the two clipboards permanently disagreeing (each suppresses the other's).
It is bounded — no loop — and a last-writer tiebreak is a design decision, not a
bug fix. AC3 asserts the bounded-ness rather than pretending the case is absent.

## 6. The Windows side — the first draft defended the wrong hazard

`OpenClipboard` failing while another process holds it is real, and bounded
retry is the right answer. **It is not the wedge.**

The wedge is *after* the open succeeds: `EmptyClipboard()` sends
`WM_DESTROYCLIPBOARD` to the previous owner — a **`SendMessage`**, not a post. A
hung previous owner blocks it **while we hold the clipboard open**, locking the
clipboard for the whole desktop. No retry policy on `OpenClipboard` touches
this, because we are already past it.

So: the open/empty/set/close sequence runs on a thread that owns nothing else,
with a watchdog that **reports** (never kills) if it has not returned within a
bound, and "clipboard sequence outstanding" becomes a state `--doctor` can show.

**Verified fine** (recorded so nobody re-litigates): a message-only
(`HWND_MESSAGE`) window does receive `WM_CLIPBOARDUPDATE`, because
`AddClipboardFormatListener` *posts* it — the message-only limitation bites the
legacy `SetClipboardViewer` chain, which we are not using. Delayed rendering is
avoided by passing real data to `SetClipboardData`, and this document forbids
"optimising" into it. A process dying while owning the clipboard is not a data
hazard: the `GMEM_MOVEABLE` handle belongs to the system after the set.

## 7. Size ceiling — new, and it is a session-safety issue

**There is no text ceiling in the codebase today, and the absence is dangerous.**
Traced: an oversize copy is framed, the receiver's `Reassembler` returns
`FramingError::Oversize` (`DEFAULT_MAX_PAYLOAD = 64 MB`), and `session.rs:197`
turns that into `SessionEnd::TransportFailed`. **Copying a large file's contents
would kill the remote desktop** — worse than the wedge this tranche prevents.

- `ClipboardPolicy` gains `max_text_bytes`, enforced as `min(policy, wire
  ceiling)` with the wire ceiling strictly below the framing limit, on **both**
  send and receive (a peer may ignore the rule).
- Over-ceiling content is **not sent and not truncated** — silently pasting half
  a document is a data-integrity bug. The local end reports once per change,
  naming sizes only.
- **Set the default at or below `HASH_PREFIX_CAP_BYTES` (256 KiB).** This is not
  arbitrary: above that cap the fingerprint hashes a prefix plus the total
  length, so a same-length edit past the prefix is **guaranteed** to be missed
  (`clipboard.rs:829`, and the test named for it). A ceiling at the cap removes
  that whole class by construction rather than documenting it as accepted risk.

## 8. Security — three leak paths the first draft missed

The clipboard carries passwords, OTPs and whole documents. The repo rule is
absolute, and the first draft's §7 was wrong in three ways.

- **The fingerprint must not be logged raw.** `content_fingerprint` is an
  **unsalted SHA-256**. For a 6-digit OTP, a PIN, or a short password the search
  space is trivially brute-forced, and the draft *also* permitted logging the
  length, which narrows it further. Anything logged for correlation is a
  per-session **salted, truncated** value; the salt is generated at connect and
  never logged. This is the tranche-3 hazard again — a mechanism declared safe
  that discloses a secret when the artefact is shared.
- **`Debug` derives are the tranche-3 pattern, and they are already live.**
  `ClipboardContent` and `ClipboardAction` derive `Debug` and carry raw content
  (`clipboard.rs:106`, `:123`). Nothing prints them today; nothing stops it, and
  the new wire type is where `debug!(?msg)` is most tempting. Every type in this
  path that can hold content gets a **hand-written `Debug`** printing kind,
  length and a salted fingerprint — never bytes. No error string may embed
  content, including third-party error text passed through. No
  `unwrap`/`expect`/indexing on content-derived data: a panic message is a log
  line you did not write.
- **"Nothing writes to disk" was false.** True of the Mac (stderr only); false
  of the server, which is where the new code lives — the agent writes
  `agent.log` and the server `server.log` on quench, unrotated, and those are
  read and quoted verbatim during diagnosis (tranche 4 §13 does exactly that).
  Every rule here applies with *more* force to the server thread, and its
  boundary catches panics rather than letting a message reach the log.

**Accepted egress, stated:** content applied to the Mac pasteboard goes to
`NSPasteboard.general`, which macOS syncs to the user's other devices via
Universal Clipboard when enabled, and to any clipboard-history manager. A user
copying to their Mac clipboard arguably expects Mac clipboard behaviour, so this
is accepted rather than fixed — but it is the largest hole in "we know where the
content goes", and a concealed-type marker is worth investigating later.

## 9. Out of scope (stated, not silently)

Images (the format byte reserves them), files, RTF/HTML, and fixing
MDR-BUG-FLUX-00011/00012.

## 10. Acceptance criteria — rewritten; the first draft's oracles could be fooled

1. **Mac → quench, live.** For each of single-line ASCII, multi-line, non-ASCII,
   and already-CRLF payloads, each carrying a **fresh nonce**: assert quench's
   clipboard does *not* already contain the nonce; set the Mac pasteboard; read
   quench's **interactive-session** clipboard back **through a path that did not
   perform the write**; assert byte-exact equality. Then repeat with the network
   path deliberately broken and assert the check goes **red** — an oracle that
   has never failed proves nothing.
2. **quench → Mac, live.** The mirror, read back through an `arboard` handle
   distinct from mdrdp's own, with the same negative control.
3. **Echo, asserted on the wire in both directions.** Exactly **one** message
   each way per user copy, and **zero** further messages over the next 8 poll
   intervals with both clipboards untouched. Both halves — a zero-only assertion
   passes on an implementation that sends nothing. Includes the A/B/A re-copy
   sequence from §5 and at least one run against the **real** quench clipboard,
   because the loop originates in OS normalisation no fake reproduces.
4. **No wedge under OS refusal**, two halves. Unit: with the sequence stuck, the
   thread's own loop keeps making progress. Live on quench: a helper holds the
   clipboard for 10 s; the thread reports, does not spin (CPU sampled), and the
   next transfer succeeds within a stated bound. `check-windows.sh` type-checks
   this code but never runs it, so the live half is the only real evidence.
5. **Oversize is refused, not fatal.** A payload above the ceiling is never
   framed, the session survives, and the local end reports size and limit.
6. **Old host degrades safely** — a safety gate, not a courtesy. Against the
   deployed 0.4.0 agent, before any tranche-5 deploy: the client reports
   clipboard unsupported, **never opens the aux socket**, sends zero upstream
   clipboard bytes on any channel, and **a keystroke still lands afterwards**,
   proving the input channel survived untouched. Same one-shot window as tranche
   4's AC5 — after the deploy there is no 0.4.0 agent left to test against.
7. **Policy on the wire.** `to_remote` off → 0 client→server messages after a
   local copy. `from_remote` off → an inbound message is discarded **before the
   payload is decoded or allocated**, and the pasteboard is byte-identical
   before and after.
8. **Soak — the requirement itself.** Board claim posted before, released after.
   ≥8 h; a ≥4 h run is a **provisional gate and must be labelled as such**, not
   a discharge of "all-day". Per cycle a transfer each way, plus hourly a
   60-transfer burst at 1 Hz and a 5-minute window with a competing
   clipboard-owner process on quench — because the wedge this product exists to
   beat appears under bursts and contention, not at one transfer per 30 s.
   Payloads carry a fresh nonce and are **synthetic by construction**, never a
   real secret, so the harness is free to record them on mismatch. Recorded and
   quoted: versions, host, start/end UTC, attempted/arrived, **arrival latency
   as n/min/median/p95/max**, every non-arrival with timestamp and direction,
   per-direction wire message counts, client RSS and queue depth sampled every
   60 s against a stated threshold, and any competing-session event. A run
   interrupted by another session is **void, not failed**. Two consecutive
   non-arrivals in one direction is a **wedge**: stop and capture state. The
   harness must never recreate the session or the clipboard handle mid-run —
   that masks the failure being tested.
9. **Hygiene**, plus a mechanical leak check: a temporary `debug!(?msg)` at the
   send site must emit no payload byte. That test starts red.

**Named as tranche work, because AC1/AC8 cannot run without it:** an agent verb
to read the interactive-session clipboard. An ssh session on Windows runs in a
different session and window station, so `Get-Clipboard` over ssh reads the
wrong clipboard. That verb is also a second `OpenClipboard` consumer and will
contend with §6's listener — the instrument perturbs what it measures, and the
design must account for it.

## 11. Risks

- **The soak is the long pole and collides with quench's one-session rule.** An
  8 h exclusive hold blocks every other live check for the whole tranche, and a
  competing connect is indistinguishable from a wedge unless the harness detects
  and voids. Worse: an RDP logon by another agent kills capture but **not** the
  clipboard path, so a naive soak would report PASS on a session that was broken
  for an hour. Sequencing must be agreed, not assumed.
- The message-only window is new Windows surface on its own thread; if its loop
  dies the clipboard degrades and nothing else may.
- A `MSG_CLIPBOARD` id must not collide with an input record kind: a stray byte
  `5` is a valid `MouseMove`, so a framing mistake would inject mouse motion
  rather than error cleanly.

## 12. Review record

Two adversarial reviewers, both against the first draft, converging
independently on the §0 blocker. **Adopted: essentially everything.** The draft
was wrong about the uplink channel, wrong about echo suppression, wrong about
which Windows call wedges, wrong that a size ceiling existed, wrong that nothing
writes to disk, and wrong that logging a fingerprint is safe. Its ACs could pass
against a dead feature via a stale clipboard.

**Refuted by the reviewers' own checking (recorded, no change):** server→client
unknown-message skipping is genuinely safe (`session.rs:217` + length-prefixed
framing); the serde-defaulted header flag genuinely costs a deployed host
nothing (`probe.rs` gates on `wire_version` only); `ArboardClipboard`'s
drop-handle-on-error property is real and load-bearing; bounded retry on
`OpenClipboard` is the right rule (just insufficient); the message-only window
and delayed-rendering avoidance are correct; AC3's "assert on the wire, not by
watching the content look right" was the right instinct.

The external Codex pass is still quota-blocked (returns 2026-08-20 04:32).

## 13. Validation record

### AC6 — old host degrades safely. **DISCHARGED 2026-08-19, and it cannot be re-run.**

Run against quench's **deployed 0.4.0 agent**, before anything from this tranche
was deployed there. That ordering is the whole point: once a tranche-5 agent
lands, no 0.4.0 host remains to test against.

Client: mdrdp **v0.1.80** (`52017df`, the integrated trunk carrying units 1–4).
Host: quench.lan.example, rhydra-agent 0.4.0, control schema 3, 1920x1080@240.
Command:

```
mdrdp quench.lan.example --native --foreground --duration 25 \
  --input-script ac6.script --metrics-json ac6-metrics.json --screenshot ac6.bmp
```

| Clause | Evidence |
|---|---|
| The client reports clipboard unsupported | stderr: `native: clipboard unsupported by this host` |
| The host genuinely predates the field | Raw header read off port 9500: `schema 5`, 24 keys, **no `clipboard` key at all** — absent, not `false`, which is exactly what the serde default has to survive |
| Never opens the auxiliary socket | The gate is `if header.clipboard`, and the field parsed false. The stronger oracle is the unit test, which counts accepts at a *listening* auxiliary port and asserts zero — a live check cannot distinguish "never opened" from "opened and closed" |
| Zero upstream clipboard bytes on any channel | `ProbeSuccess.aux` is `None` and nothing yet wires the clipboard into the session; the 86 records written were input |
| A keystroke still lands afterwards | `win+r` opened Notepad on the remote desktop and typed characters appeared in it (screenshot). The agent's pool `frame_seq` went **29 → 188** across the run, on a desktop the liveness rung had just reported static |
| The session itself was healthy | 37 frames, 286231 bytes in, **0 decode errors**, clean `WindowClosed` exit |

**Honest caveat on the screenshot:** it shows `key`, not the full typed string.
The 25 s budget cut typing short — Notepad took longer to take focus than the
script allowed. That is script timing, not lost input; the chord, the `type` and
the `enter` that opened Notepad all demonstrably landed.

**Also confirmed, and it is a pre-existing open defect:** the metrics report
shows `latency.sample_count: 0` (MDR-BUG-FLUX-00012). Worth noting that
`decode`, `present` and `frame_gap` *were* populated this run (37/44/43
samples), so that ticket is narrower than its title suggests — it is the
keypress-to-photon latency arm that is empty, not all stats.

### Getting the host there — and a defect it exposed

quench was not usable when this started: `--doctor` reported `device`, `pool`,
`display-mode` and `server` all FAIL. Two independent faults were stacked.

1. **The stranded pool** was real — the capture server held generation 1 while
   the driver published 2. `cycle-device` cleared it.
2. **The device rung was lying.** The IDD driver was present and healthy
   (`SWD\MDRDP_IDD\MDRDP_IDD`, status OK) the whole time. The real fault was that
   the console had moved to an empty session 4 while the agent sat in ano's
   *disconnected* session 2 — and a disconnected session enumerates no displays.
   The rig's existing `mdrdp-tocon` task took every rung to `ok` in one step.

Raised as **MDR-BUG-FLUX-00017**: the ladder cannot distinguish "no IDD device"
from "my session is disconnected", the remedies are opposites, and it names the
destructive one. The correct diagnosis was already in the report — under
`input-desktop`, two rungs *below* the symptom.

### First live run — the clipboard works both ways

Client mdrdp **v0.1.87**, host **rhydra 0.5.0** deployed to quench
(`C:\mdrdp\v0.5.0\`), 1920x1080@240, agent stably green.

**The channel is real.** quench listens on 9500/9501/9503 from one process, and
the header it now sends is schema 6 with 25 keys including `clipboard: true` —
read straight off the wire, not inferred. Every session reports
`native: clipboard ready`.

| Direction | Evidence |
|---|---|
| **host → Mac** | `HOSTTOMACK9P2` typed into Notepad on quench and copied there arrived in the Mac's `pbpaste`, byte-exact (`0a 48 4f 53 …`). That string existed nowhere else. |
| **Mac → host** | `MACTOHOSTZ8W1` copied on the Mac *fifteen seconds into the session* pasted into Notepad on quench and is visible in the session screenshot. The host's own log agrees: `aux: disconnected — 1 applied, 0 refused by policy, 0 malformed, 0 of an unknown type`. |

Sessions: 33–37 frames, **0 decode errors**, clean `WindowClosed` exit each time.
Nothing in `win::clipboard` had ever executed before this run.

### A user-visible consequence of seeding, found the hard way

**Content already on your clipboard when you connect is never transferred.** The
first attempt set the Mac pasteboard *before* connecting and nothing moved — the
host logged `0 applied`. That is not a defect: §5 seeds both slots at connect
precisely so the first poll does not read as a change and clobber the other end,
with which end wins decided by a race. The cost of that decision is this, and it
had not been written down as *behaviour* anywhere.

It is stated here because it will look like a bug to a user, and because a test
that copies before connecting proves nothing. **Copy after connecting.**

Two smaller notes from the same runs:

- `win+r` opens a **tab** in the existing Notepad on Windows 11, so scripted
  keystrokes went to whatever had focus. Clicking into the text body first is
  what made the visual proof work. An input-focus matter, not a clipboard one —
  worth separating, because conflating them would have read as a clipboard bug.
- The host log renders the em-dash in `aux: disconnected — …` as mojibake in the
  Windows console codepage. Cosmetic, but `server.log` is read and quoted during
  diagnosis, so host log strings should stay ASCII.

### AC1 and AC2 — DISCHARGED 2026-08-19

Client mdrdp **v0.1.88**, host **rhydra 0.5.0**, quench all rungs green.

The instrument both criteria needed is `mdrdp <host> --clipboard-check <text>`,
which asks the agent — the only process in the console session — whether the
interactive clipboard holds exactly that text. **It compares rather than reads.**
The verb the criterion implied ("read the host's clipboard") would pull arbitrary
content down a control port anything on the host can reach; a caller asserting
equality already knows the text, so the expectation goes in and a boolean plus
two lengths come back. Both sides are canonicalised, because the host stores
CRLF and a raw comparison would fail on every multi-line payload for a reason
unrelated to whether the clipboards agree.

**AC1 — Mac → quench, byte-exact. 4/4.** Every payload carried a fresh nonce and
was asserted **absent from quench before the copy**, so the oracle demonstrably
fails when it should:

| Payload shape | Result |
|---|---|
| single-line ASCII | match, 26 bytes |
| multi-line (real newlines) | match, 43 bytes |
| non-ASCII (`café—naïve-日本語-🎉`) | match, 44 bytes |
| already-CRLF | match, 31 bytes |

The negative control is not decoration: the very first run of it reported
`MISMATCH (host holds 13 bytes, expected 19)` against a leftover payload, which
is what a working oracle looks like before the thing it measures happens.

**AC2 — quench → Mac. 2/2**, read back through `pbpaste`, a path that did not
perform the write. Single-line (19 bytes) and multi-line (29 bytes); the
multi-line case is the one that matters, because it proves the host's CRLF
arrives on the Mac as LF. Each was asserted absent from the Mac first.

The host payloads are alphanumeric on purpose: `autoinput` emits US scancodes
and quench is a UK layout, so punctuation would arrive as something else and the
test would fail for a reason that has nothing to do with the clipboard.

**Still open:** AC3 (echo counted on the wire), AC4 (no wedge under OS refusal),
AC5 (oversize refused, session survives), AC7 (policy on the wire), AC8 (the
soak), AC9 (hygiene plus the mechanical leak check).

### AC3, AC5 and AC7 — DISCHARGED 2026-08-19

All three are claims about the **wire**, not about a clipboard: "exactly one
message each way per copy", "never framed", "zero client→server messages".
Looking at a clipboard afterwards cannot tell one message from three, or from
none plus a coincidence. So the native session now reports four counts at
teardown — sent, applied, echoes suppressed, refused — and these criteria are
asserted from them. Counts carry no content, which is why they are the one piece
of clipboard telemetry that is safe to print.

**AC3 — echo, on the wire.** Three copies in one session in an **A/B/A**
sequence, then eight poll intervals of quiet with both clipboards untouched.
Result: `sent=3`, and nothing further. The A/B/A shape is the point: re-copying
A after B is exactly the case the rejected suppression design lost for ever, and
a design with that bug would report `sent=2`.

**AC5 — oversize refused, not fatal.** A 262194-byte copy (50 over the ceiling)
made *during* the session: `sent=0`, the session exited 0, and the local end
reported `clipboard not shared: 262194 bytes is over the 262144-byte limit`. The
report was also checked for payload bytes and contains none.

**AC7 — policy on the wire.** With `direction = "from_remote"` (so `to_remote`
off), a local copy produced `sent=0`, and `--clipboard-check` confirmed quench
does not hold it. Two independent oracles, one at each end.

#### Both failures on the first attempt were the harness, not the code

Worth recording because both would have been reported as defects by a less
suspicious run:

- **AC5 first read `sent=0, refused=0` with no report at all.** The oversize
  copy had been made *before* connecting, so seeding correctly suppressed it —
  the same trap as the first live run. Any clipboard test that copies before
  connecting proves nothing.
- **AC7 first read `sent=1`**, which looks exactly like a broken direction gate.
  The cause was my own settings file: I wrote `direction = "from-remote"` where
  the enum is `snake_case` and wants `from_remote`. The file failed to parse and
  mdrdp fell back to defaults.

That second one is a real defect in its own right — **MDR-BUG-FLUX-00018**: a
malformed `settings.toml` falls back to `Settings::default()`, and the clipboard
default is `Both`, so a typo silently turns a *restriction* fully permissive. A
GUI user never sees the stderr warning.

**Remaining: AC4** (no wedge under OS refusal, which needs a helper holding
quench's clipboard), **AC8** (the soak), and **AC9** (hygiene plus the
mechanical leak check).


### AC9's leak check and AC4's unit half — and the three defects they found

Both criteria were written as tests, and **both started red against real bugs**.
That is what they were for, and it is the strongest argument in this tranche for
writing acceptance criteria as executable checks rather than as prose.

**AC9 — the mechanical leak check.** `Take` derived `Debug`, so a `debug!(?take)`
anywhere in the writer loop printed the whole clipboard — a password, a one-time
code. That is the **tranche-3 pattern exactly**: a type whose derived `Debug`
looks innocuous and carries content, written two units after that lesson was
recorded. Hand-written now, like every other type on the path.

**AC4 — a stuck clipboard write must not stop the poll loop.** `EmptyClipboard`
blocking on a hung previous owner cannot be prevented, so the only question is
whether it takes the session with it. It did, in two independent ways, and the
second survived the fix for the first:

1. **One clipboard handle shared behind a mutex.** A blocked write held it; the
   poll thread stalled behind it for the whole block. Each thread now gets its
   own handle, so the OS serialises them the right way — the poll's
   `OpenClipboard` fails fast under contention, bounded retry gives up for that
   lap, and a read failure deliberately leaves the suppression slot alone.
2. **The bridge lock held across the write.** The same failure one level up.
   `apply_remote` and `poll_local` now take `&Mutex<Bridge>` and lock only to
   decide — the discipline is in the signature rather than in a comment, and the
   tests exercise that shape rather than a simpler one.

**A third defect the same test found**, and the subtlest: `on_remote_text`
recorded the applied text *before* the write. While a write is stuck the bridge
believes the clipboard already holds the new text, so the poll reads the **old**
content, finds it different from the record, and sends it back as though the
user had copied it — clobbering what they actually copied. Deciding no longer
records; the slot moves only once the clipboard has taken the text.

That last change makes `note_applied` genuinely load-bearing, so a failed
read-back now falls back to the applied text: one redundant message the peer
recognises as its own, against sending the *previous* clipboard back.

**AC4's live half is still outstanding** — a helper holding quench's clipboard
for 10 s, with CPU sampled to show the thread reports rather than spins.

### AC4 — unit half DISCHARGED, live half PARTIAL

**Unit half: discharged**, and it earned its keep — the three defects above were
all found by it.

**Live half: two of three clauses proven, one not.** Client v0.1.91, host rhydra
0.5.0 on quench, 46 s and 60 s runs with a PowerShell helper attempting to hold
the clipboard.

| Clause | Result |
|---|---|
| does not spin (CPU sampled) | **Proven.** 0.39 CPU-seconds across a 46 s run, sampled from `rhydra-server` before and after. A spinning retry would have burned seconds. |
| the next transfer succeeds | **Proven.** Byte-exact via `--clipboard-check`, and the host's own summary agrees. |
| the thread **reports** the refusal | **Not proven.** |

**Why the third clause is not proven, stated rather than glossed:** the helper
does not actually hold the clipboard. Its own `CloseClipboard` returns **False**
— it had already lost it — and the host duly reported `write-failed 0` with the
write succeeding. So no refusal occurred, and a run that "passed" would have
been measuring nothing.

The first attempt was worse and instructive: the copy landed *before* the helper
opened at all, because `Add-Type` compiles C# on first use. Synchronising the
copy on the helper's own log fixed the timing and revealed the deeper problem.

What would fix it is a helper that genuinely holds the clipboard for a bounded
period — a small native binary rather than a PowerShell P/Invoke, whose
apartment and lifetime semantics are what appear to be losing the handle.

**This is the weaker half of the evidence and it is worth saying why it matters
less than it looks:** the unit half tests *our* response to a refusal, which is
the thing under our control, and does so deterministically. The live half would
only add confidence that Windows refuses when we think it does.

#### The instrument was wrong before the test was

Running this found that **the host's per-connection summary counted decodes and
called them applications**. During the hold it reported "2 applied" and could
not distinguish a write that landed from one the clipboard refused — precisely
the distinction AC4 exists to make. `serve_one` now returns what `apply_remote`
actually did, and the log names written / echo / write-failed separately.

Also fixed there: the summary is ASCII now. `server.log` is read through the
Windows console codepage, where the em-dash came out as mojibake, and that log
is quoted verbatim during diagnosis.
