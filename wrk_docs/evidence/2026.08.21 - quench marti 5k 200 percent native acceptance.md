# Quench `marti` 5K/200% native acceptance

Date: 2026-08-21

## Installed state

- Integrated client: mdrdp v0.1.114 (`origin/main` `e353141`). The installed
  `~/.local/bin/mdrdp` matched the integrated release binary by SHA-256.
- Host stack: rhydra v0.5.0, wire v5, IDD driver 0.3.0.1.
- Windows console: `marti`, session 3, active. `ano`, session 2, disconnected.
- Scheduled task: `rhydra-agent`, interactive-only, run as `marti`.
- `rhydra-agent`, `mdrdp-idd-create`, and `rhydra-server` all ran from
  `C:\mdrdp\v0.5.0` in session 3.
- The recoverable previous install remains at
  `C:\mdrdp\backup-v0.5.0-ano-20260821`. No reboot or credential change occurred.

## Effective-scale regression

The first integrated DPI-awareness fix made monitor lookup work but returned 180% after
Windows Display Settings had selected 200%. The replacement in `0ce21e5` measures the
effective DPI assigned to a short-lived PMv2 window on the IDD.

- The Windows-only conversion regression was run on Quench. Its stub failed the exact test
  (`None` instead of `Some(100)`); the implementation passed the same exact test.
- A freshly deployed agent reported 200% where the prior API reported 180%.
- Without restarting the agent, supported Display Settings automation changed the IDD from
  200% to 100% and back to 200%. Agent uptime advanced from 302s through 521s while status
  tracked both values.
- Final status reported 5120x2880 at 240 Hz, actual and desired scale 200%, all health rungs
  green, pool/server generation 4, and no viewer.

## End-to-end acceptance

- `mdrdp Quench` resolved `quench.lan.example`, selected native transport without identity or
  transport flags, requested 5120x2880 at 240 Hz and Windows scale 200%, and connected on
  wire v5. The saved favourite keeps `user@example.com`, fullscreen, and
  `native = "always"`; it stores no SSH user.
- A controlled fullscreen session ended `WindowClosed` after 31 frames, 2,659,873 bytes,
  and zero decode errors. It delivered 2,079 audio frames with zero queue drops/capture
  gaps and measured a real stereo render at about 437 Hz left / 659 Hz right.
- A post-connect synthetic clipboard change produced `clipboard sent 1`; the host-side
  read-only verdict matched 33 bytes. Clipboard content is not retained here.
- Final `mdrdp quench.lan.example --doctor` reported SSH, tunnel, agent, creator, device,
  pool, display mode, server, input desktop, liveness, audio, and no-viewer checks all OK.

## Local gates

- Portable rhydra tests: 299 passed.
- Rhydra clippy: warnings denied, clean.
- `scripts/check-windows.sh`: clean, including the host-enabled Windows crate.
- Windows all-target test compilation: clean.
- Integrated Windows release server build and integrated macOS release client build: clean.

Independent ledger verification is recorded in `bugs/MDR-BUG-FLU-00028.md`.
