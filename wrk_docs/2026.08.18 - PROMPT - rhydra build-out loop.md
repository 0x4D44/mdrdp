# PROMPT — rhydra build-out loop

The standing prompt for the self-paced loop that builds out the rest of the
rhydra native stack. Invoke as:

```
/loop Follow "wrk_docs/2026.08.18 - PROMPT - rhydra build-out loop.md" in the mdrdp repo.
```

Re-read this file at every loop entry — it, the tranche journals
(`wrk_journals/2026.08.18 - JRN - rhydra tranche N build.md`), and the parent
HLD (`wrk_docs/2026.08.18 - HLD - native product shape and session
ownership.md`) are the loop's memory. Load the `loop-build` skill.

## Mission

Deliver steps 3–7 of the parent HLD's §9 sequencing, one tranche per 2L
cycle, integrating each when green:

3. `mdrdp <host> --native` MVP — ssh spawn + forwards, spike-viewer presenter
   embedded in mdrdp's window shell, and the **auto-detect connect path**:
   prefer rhydra when the probe finds it, fall back to RDP.
4. Health ladder + `--doctor` (upgrade detection from "port answers" to rungs).
5. Clipboard (text-first, both directions, parent HLD §6).
6. Audio (after clipboard; RDPSND-equivalent).
7. IddCx-direct capture promotion — **decision, not foregone**: gate on the
   measured tail; "don't, with evidence" is a valid landing.

Also owned by the loop, opportunistically:
- Retro external Codex reviews (tranche 1 + 2 HLDs, then each new HLD) once
  quota returns 2026-08-20 04:32; fold verdicts into the HLD review records.
- The deferred measurement arms — motion/scroll and the three-way glass run
  (spike-viewer vs mdrdp vs Windows App 11.3.8) — which feed tranche 7's
  gate. They need a hands-off, unlocked Mac; run them in idle windows.
- Rig retirement on quench: disable the superseded `mdrdp-server-*` /
  `mdrdp-idd-create` rig tasks (the agent owns the stack now). Disable, don't
  delete; keep the utility tasks (prep, winlist, tocon, shot).

## Per-tranche flow (the proven 2L light flow)

Worktree via `deltic start-task` → understand (read the parent HLD section +
prior journals) → author the tranche HLD in `wrk_docs` → 2–3 adversarial
reviewers (refutation standard; reclaim inline on API failure) → attempt the
external Codex review (journal if it cannot run; never block) → mark
SELF-APPROVED, commit, proceed → implement tests-first (every new test made
to fail once — restore mutations from a saved copy, never `git checkout`) →
fmt, clippy -D warnings, focused tests, `./scripts/check-windows.sh`, cross
builds where the server/driver changed → live validation on quench →
journal → integrate.

## Standing authority (granted by Arthur launching this loop)

- **Integrate (push) each tranche when its acceptance criteria are green and
  the journal + HLD validation record are written.** Docs-lane commits
  auto-integrate as usual. Never force-push; never tag or release.
- Deploy refreshed host binaries to quench with `mdrdp deploy` as needed.
- Use quench freely under board etiquette (below).

## Settled answers — do not re-litigate

- rhydra is fleet-only, never published; the name is rhydra (prose may say
  Hydra). Driver stays C++; self-signed cert is permanent.
- Auto-detect is the default; deploy is the opt-in. Per-favourite
  `native = auto|always|never` + `--native`/`--rdp` overrides.
- Transport is always visible (title/HUD/stage-json); **no silent mid-session
  fallback** — transport is chosen at connect time only.
- v1 leaves the physical console as-is while rhydra owns it (§8 Q2: ignore).
- dxgi stays the default capture source (decision 7, re-confirmed under the
  diff 2026-08-18); IddCx-direct is tranche 7's question.
- Versioned host dirs under `C:\mdrdp\`; stable-green is the health rule.

## Hard stops — end the tranche and leave a note for Arthur instead

- **Never write auto-logon credentials or any logon config** (classifier and
  policy; the unattended-reboot criterion stays open until Arthur acts).
- No new dependencies without Arthur's word (journal the proposal, continue
  with what exists or park the tranche).
- No rebooting quench unless the host is genuinely wedged and the journal
  says why. Never work around a hook/guard refusal.
- A genuine architectural fork (parent HLD amendment, scope change, anything
  contradicting a settled answer) parks the tranche with a written question.

## Environment realities

- **Quench etiquette**: post a board claim before taking the box, release
  when done; one session per account — check `mdrdp --sessions` and the board
  before long runs; always disconnect gracefully; `query session` is the
  first diagnostic when the IDD display "vanishes" (an attached RDP session
  replaces the console display topology).
- **The Mac can lock.** Screen capture and window focus (glass runs, viewer
  A/Bs) stall on a locked Mac — `caffeinate -dis` for the run, and if the
  display is locked anyway, re-queue the GUI validation and advance a
  non-GUI tranche instead. Never count a stalled GUI check as failed.
- Commit completed work before starting any long validation. Background
  waits need an armed wake-up. Bound every long command with
  `deltic timeout`; run test suites with stdin closed.
- Credentials stay in the macOS keychain (service `mdrdp`); ssh to quench as
  `ano` (password = username, throwaway; SSH_ASKPASS pattern in the journals).

## Loop mechanics and pacing

- Dynamic pacing via ScheduleWakeup: work continuously while there is work;
  when blocked on an external window (Codex quota, locked Mac, Arthur's
  answer), schedule a wakeup matched to that window and `noop` honestly.
- Each entry: read this file, the newest journal, `git log --oneline -10` on
  origin/main; pick up the open tranche or start the next; never redo landed
  work.
- Stop the loop (ScheduleWakeup stop) when: tranches 3–6 are integrated,
  tranche 7's decision is landed (built or declined with evidence), the retro
  reviews are done, and the final journal entry + board post summarise the
  build-out. Leave remaining open items (auto-logon, §8 Q1) in that summary.
